node_modules/
dist/
*.tsbuildinfo
.tsbuild/

# Build + test output
coverage/
playwright-report/
test-results/
# Vitest browser mode writes these only when an assertion fails; they are debugging
# output for the run that produced them, not something to carry in the repo.
__screenshots__/
.vitest-attachments/
apps/host-demo/dist/

# Pack test scratch area (created outside the workspace, but ignore any stray local copy)
.pack-scratch/
*.tgz

# Secrets. Nothing here today, and this exists so that stays true: the repository is
# public and CI holds a publish token.
.env
.env.*
!.env.example
*.pem
*.key

# Editor / OS
.DS_Store
.idea/
.vscode/
