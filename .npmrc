; Fail the install when a package's declared engines are not satisfied. The Node floor
; in ADR-0010 is a published compatibility contract, so it should break loudly here
; rather than silently in a consumer's install.
engine-strict=true

fund=false
audit=false
