# Feature-Parity Checklist

- Area: Acceptance ledger for SurveyJS feature parity
- Status: active
- Owner: Jarod
- Last updated: 2026-08-04

This is the authoritative acceptance document for the project's overall AC. A row is
**green only when the named proof passes in CI** — a host-demo scenario, rendering
integration test, or unit suite exercised through public APIs (see the
[guidelines](./library-development-guidelines-details.md)). Never flip a row by
assertion.

Parity target is the SurveyJS **Form Library** and **Survey Creator** as documented
in 2026 (v2/v3 era). Items on SurveyJS's own 2026 roadmap that have not shipped as
core parity surface are tracked in §O as *watch* items, not acceptance items.

Proof naming convention: `parity/<row-id>-<slug>` (e.g. `parity/B3-visible-if`).

**Demo coverage rule.** A closed row carries a host-demo scenario whenever the
capability is observable in the running application. Where it is not, the row says so
and why. Unit coverage alone is the exception, not the norm — the demo is what proves
the capability through the same boundary a consumer uses, and it has repeatedly caught
what unit tests could not: a numeric answer that rendered blank, an API wart, a
placeholder that never cleared.

> **Vocabulary caveat (added 2026-08-02).** This checklist was derived from SurveyJS's
> documented surface and therefore uses their property and type names throughout —
> `visibleIf`, `choicesByUrl`, `paneldynamic`, `matrixdropdown`, `questionsOnPageMode`,
> `storeDataAsText`, and so on. Since
> [ADR-0001](./adr/0001-own-definition-format.md) established that our definition
> format is **our own**, those names describe the **capability a row demands, not the
> name our format must use**. Do not treat a name in this document as a naming
> requirement — read every identifier here as "the capability SurveyJS calls X".
>
> These rows are restated **incrementally**: when a PR declares the corresponding
> property in the metadata registry, that same PR rewrites the row in our own
> vocabulary. There is no separate rename pass, because naming properties in this
> table ahead of the registry would invert the design order ADR-0001 sets. This banner
> is removed once §A–§N are clean, and its presence is the outstanding-work marker.
>
> **Still present with §A–§N green, deliberately.** Every row from §A to §N now closes,
> N5 last. What has *not* happened is the restatement this banner is about: the Feature
> column still reads `matrixdropdown`, `storeDataAsText`, `showJSONEditorTab` and the rest,
> because that column states the capability a row demanded rather than what shipped, and
> nothing rewrote it as each row landed. Removing the banner on the strength of the Status
> column would claim a migration that has only happened inside the Proof column. The
> restatement is the outstanding work, and this is its marker.

## §A — Schema, serialization & extensibility

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| A1 | JSON survey definition parses into a typed model; unknown properties surfaced, not dropped | ☑ | `parity/A1-unknown-properties-surfaced` (unit + host E2E) |
| A2 | Lossless round-trip: parse → serialize → parse is stable for every fixture | ☑ | `parity/A2-round-trip` (unit, every fixture) + `parity/A2-round-trip-fixed-point` (host E2E) |
| A3 | Metadata registry: register question class with typed property descriptors, defaults, inheritance | ☑ | `parity/A3-metadata-registry` (unit). The descriptor is the only place a default is written, and the model resolves through it ([ADR-0016](./adr/0016-metadata-owns-property-defaults.md)) — so "unset" and "explicitly empty" are now different states, and a directly constructed `new TextQuestion()` resolves the same defaults `parseSurvey` would give it. Not demo-testable: the registry is machinery with no user-visible surface of its own |
| A4 | Custom question type registers end-to-end (serializer, schema, toolbox, property grid, renderer) | ☑ | Closed by §L1, which was the one leg missing. Serializer and schema in `packages/core`; toolbox in `parity/K1-toolbox-registry` ("a custom type appears with nothing added to the toolbox"); property grid in `parity/L1-grid` ("a custom question type brings its own properties"); renderer in `parity/A4-custom-question-renderer`, through the unified page-element registry in real Chromium. **Five legs, five proofs, and no single scenario walking all five** — stated rather than implied by a row that reads as one. **That gap is now closed by §N5**, which builds a survey covering every registered type through the assembled Creator, renders it, answers it and brings it back; the claim each of these tests makes on its own is still the narrower one, that the mechanism auto-populates from the registry |
| A5 | Custom property on an existing type (`addProperty` pattern), serialized and editable | ☑ | Closed by §L1. `addProperty` serializes and reaches the JSON Schema contract (`packages/core`), and `parity/L1-grid` proves both halves of "editable": a property added before the survey was parsed appears in the grid with its label, editor and description derived from the descriptor, and one added *after* it still shows its registered default — defaults are captured on an element when it is created, so an element that already existed has never heard of the new property and the row has to fall back to the descriptor. Without that, a host extending a running Creator would see an empty field where the registry says there is a value |
| A6 | Committed JSON Schema contract generated from the registry; CI drift check | ☑ | `parity/A6-contract-generated-from-registry` (unit) + `check:contract` CI job. Not demo-testable: the contract is a build artefact, not application behaviour |
| A7 | Typed event surface: onValueChanged, onComplete, onCurrentPageChanged, onValidate*, onUploadFiles, onDynamicPanelAdded, matrix row events | ☑ | `parity/A7-value-changed-event` (demo) for value/complete/page; `parity/D4-validate-question-event` and `parity/D3-async-validators` for the validation pair; `parity/A7-records-changed` and `parity/A7-files-changed` (unit) plus `parity/A7-model-events` (demo) for the three that had to wait for their features. **One channel for matrix rows and repeating-panel instances**, not two: §F and §G already share `RepeatingQuestion`, and a host reacting to rows almost always means instances — two channels is one of them forgotten the day a third repeating type arrives. Which kind it is, is a fact about `question`. Emitted from the model where the record actually changes, so a host hears it however the change was caused, and *not* from `invalidateCells`, which also fires for a rebuild that changed nothing: a locale switch throws every instance away, and announcing a row added because somebody switched to French is a lie a host acts on. **`onFilesChanged` is not the upload seam.** Where a file is stored is `SurveyOptions.uploadFiles`, because only the host knows; this is the notification the answer changed, which anything may watch — a partial save, an audit log, a second view of the same response. A host that conflated them would find its storage called twice the day something else wanted to know. A failed upload announces nothing, because the answer did not change and a listener told otherwise would save a reference to a file nobody stored; a successful one reports **what the host gave back** rather than what went in, since the reference it minted is the thing anything downstream can use. The demo subscribes the way a real host does — no renderer, no callbacks through props — and prints what arrives |

## §B — Expressions & conditional logic

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| B1 | Expression language: literals, `{question}` refs incl. composite paths (`{matrix.row.col}`, `{panel[0].q}`), arithmetic, comparison, and/or/not, contains/anyof/allof, empty/notempty | ☑ | `parity/B1-expression-grammar` (precedence/associativity via print round-trip) + `parity/B1-operators` (evaluation), unit, through public API. Not demo-testable: precedence, associativity and error recovery have no user-visible surface — the demo exercises the language's *use* in every other scenario |
| B2 | Built-in function library (iif, sum, avg, min, max, count, age, today, currentDate, getDate, diffDays, ...) + custom sync and async function registration | ☑ | `built-in function library` for the library, `parity/B2-async-functions` (unit + demo) for registration. Building this found that **a host had no way to register a function at all** through `parseSurvey` — the sync half was reachable only by calling the evaluator directly, which no host does — so both halves now arrive through the parse options. The evaluator stays synchronous: an expression is evaluated inside a dependency transaction, and one that stopped to await would leave every rule after it unevaluated and the model part-way through a cascade. So an async call yields `undefined` this time round — which every operator already treats as an unanswered question, so no operator needed a third state — the work starts, and the rules run again when it lands. **The result cache is load-bearing, not an optimisation**: without it each re-evaluation starts the call again and each call triggers another re-evaluation, and the mutation proving that hung the harness rather than failing it. Keyed on name *and* arguments. A rejection is reported at the call site's own span and not retried, so a broken endpoint is not a request storm. One namespace, checked in both registration orders. **Worth knowing:** every async function is called once at startup with whatever its arguments are then, so a lookup that costs something should return early for an argument it cannot use — the demo's does |
| B3 | `visibleIf` on questions, panels, pages, and individual choices | ☑ | `parity/B3-visible-if`, `parity/B3-visible-if-choice` (questions, pages, choices) and `parity/E1-panel-visibility` (panels, unit + browser + host E2E). A hidden panel takes its subtree out of reach without marking each child invisible — reachability and visibility are different questions, and only the first one is the container's to answer |
| B4 | `enableIf` / `requiredIf` | ☑ | `parity/B4-enable-if`, `parity/B4-required-if` (unit, browser, host E2E). Scope of `requiredIf`: questions, the only element that holds an answer. `enableIf` now also applies to panels, proven by `parity/E1-panel-enable` — the renderer uses a `fieldset`, so a disabled group freezes its subtree natively rather than by walking children |
| B5 | `setValueIf` + `setValueExpression`, `resetValueIf`, `defaultValueExpression` | ☑ | `parity/B5-default-value-expression` (unit + host E2E), `parity/B5-set-value-if` and `parity/B5-reset-value-if` (unit + host E2E). Precedence reset > set > default is defined in `createValueRule`, not left to graph ordering |
| B6 | Calculated values (survey-level `calculatedValues`, usable in expressions and completed HTML) | ☑ | `parity/B6-calculated-values` (unit + host E2E) covers computation, chaining, use from question expressions, and `includeIntoResult`; `parity/B6-calculated-values-in-completed-html` (unit) and `parity/E5-completion-flow` (demo) close the completed-HTML half, which had nowhere to be proven until E5 existed. A completed page reads a calculated value and an answer through the same placeholder and cannot tell them apart — including a value *not* marked `includeIntoResult`, because what an ending says is a different question from what gets submitted |
| B7 | Triggers: complete, setvalue, copyvalue, runexpression, skip | ☑ | `parity/B7-trigger-complete`, `-setvalue`, `-copyvalue`, `-runexpression`, `-skip` (unit); host E2E for all five. Triggers fire on the transition into true, not while it holds. `skip` became demo-testable once the renderer paginated (§E2): `parity/B7-trigger-skip` moves the respondent back a page and the renderer follows |
| B8 | Dependency graph: value change re-evaluates only dependents; cycles detected and reported | ☑ | `parity/B8-dependency-graph` (selective re-evaluation, ordering), `parity/B8-cycle-reporting` (participating nodes named), `parity/B8-pattern-edges` (dynamic collections), unit, through public API. Not demo-testable: "only dependents re-evaluated" is by definition invisible, and proving cycle reporting would mean authoring a broken survey into the demo |
| B9 | Carry-forward choices (`choicesFromQuestion`, selected/unselected modes) | ☑ | `parity/B9-carry-forward-choices` (unit + host E2E). The carried list is a live view, not a snapshot, so a source choice hidden by its own `visibleIf` disappears here too. Shares `ChoiceSourceController` with B10, so both dynamic sources resolve through one object |
| B10 | REST choices (`choicesByUrl`: url/path/valueName/titleName, caching, url with `{question}` placeholders) | ☑ | `parity/B10-rest-choices` (unit + host E2E). Placeholders become real graph dependencies, so an answer change re-fetches. The fetcher is injected — core is I/O-free and cannot reference `fetch`. The demo calls a live public API; the E2E intercepts it so CI never depends on a third party. A superseded response cannot install itself over a newer one (request generations, proven by mutation), the cache is keyed on the conversion settings and not the URL alone, and one in-flight request is shared by every source awaiting it. The original typed value survives the renderer, so a numeric `id` records as a number. Definitions carry origin-relative URLs and the host resolves the origin in `fetchJson`, so one definition promotes across environments unedited ([ADR-0017](./adr/0017-choices-url-environment-portability.md)); the demo's absolute URL demonstrates a public third-party API, not deployment portability. **Gaps noted:** the model carries no loading flag, so a renderer cannot show a loading state; multi-origin definitions are B11 |
| B11 | Deployment variable scope: `{@name}` placeholders resolved from host-supplied endpoints, distinct from the answer scope | ☑ | `parity/B11-deployment-scope` (unit). Built to [ADR-0017](./adr/0017-choices-url-environment-portability.md): substituted **verbatim** because it is a URL prefix and not a value, while an answer in the same URL is still percent-encoded — opposite treatment on purpose, since an encoded origin is a broken request and an unencoded answer is a way to reach a different host. Never a graph dependency: it is constant for the session. A separate namespace, so collision with a question name is impossible by construction. An undeclared name is a **parse diagnostic at error severity** — and, a test found, that alone was not enough: substituting the empty string left `/users`, which the fetcher then sent to the app's own origin, so a URL naming an origin nobody supplied is now not fetched at all. The template round-trips; the resolved origin never serializes. **Unproven guard:** the filter keeping `{@name}` out of the dependency graph. Its effect is a graph node that never fires, which has no observable behaviour — the mutation removing it survives, and inventing a test that could see it would be testing the test |

## §C — Question types: core

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| C1 | text — all inputTypes (text, number, email, date, datetime-local, time, tel, url, color, range, password), min/max, step, ~~maskSettings~~ | ☑ | `parity/C1-input-types`, `parity/C1-text-bounds`, `parity/C1-numeric-answers-are-numbers` (unit + demo) cover every input type, `min`/`max`/`step`, and the rule that a numeric input stores a number rather than the string the DOM reported. **`maskSettings` is dropped from parity scope**, not deferred ([ADR-0018](./adr/0018-input-masking-out-of-scope.md), 2026-08-02): masking is a caret-management problem wearing a formatting problem's clothes, the model already owns the part that matters — what reaches `data` is a typed value either way — and a host that needs one plugs in a library through the renderer seam rather than taking a nearly-right implementation from us. This is the **first named gap against the SurveyJS Form Library**, and it is stated here rather than implied by a row that looks complete |
| C2 | comment (multi-line, autoGrow, character counter) | ☑ | `parity/C2-comment` (unit + demo), `parity/C2-comment-auto-grow` (demo, measuring real height in Chromium). The budget is enforced by the model, not just displayed: a trigger or a restored `data` payload can exceed a `maxlength` attribute |
| C3 | radiogroup (choices, otherItem, noneItem, showClearButton, colCount, choicesOrder) | ☑ | `parity/C3-radiogroup` (unit), `parity/C3-C4-select-questions` (host E2E), browser proof for selection. `choicesOrder` supports none/asc/desc; random is deferred as it would make the suite non-deterministic |
| C4 | checkbox (selectAll, none, other, maxSelectedChoices, colCount) | ☑ | `parity/C4-checkbox` (unit), `parity/C3-C4-select-questions` (host E2E), browser proof for multi-select |
| C5 | dropdown (search/filter, lazy loading, placeholder, showOtherItem) | ☑ | `parity/C5-dropdown` (unit + browser) for single-select, placeholder and showOtherItem; `parity/C5-lazy-loading` and `parity/C5-lazy-search` (unit + demo) for paging. The host supplies a `loadChoicePage` loader — a callback rather than a URL template, because paging needs an offset, a size and a term against an API only the host knows the shape of, and core carries no I/O. Pages **accumulate**: the respondent is looking at page one, and a list that emptied itself to show more would lose their place. The end of the list is what the host reports rather than what a short page implies — a server-side match can legitimately be thin — and a mutation that inferred it from the page length is killed by a loader that under-fills. Search goes to the host, because the matching entry may never have been loaded; a reply for a term the respondent has typed past is discarded, proven by answering the requests out of order. A page that fails to arrive leaves the list usable and retryable rather than loading forever. Pagers outlive rule registration, which is rebuilt whole on every tree change. **The scroll trigger is an explicit control, not a sentinel**: a native `<select>` has no scroll to observe and infinite scroll reaches nobody who is not pointing at the list — Phase 2's combobox adds the scroll trigger alongside the button, never instead of it. The searchable combobox *UI* stays deferred to that a11y pass; the filter is a labelled search field beside a native list |
| C6 | tagbox (multi-select dropdown with search + lazy load) | ☑ | `parity/C6-tagbox` (unit + browser in real Chromium, proving the native adapter preserves the model's invariants) and `parity/C6-lazy-tagbox` (unit). The adapter reports its whole selection and the model decides what that means, so none-exclusivity and `maxSelectedChoices` hold however the selection was made. Paging lives on `SelectQuestion` rather than on either subclass, so a tagbox pages on identical terms — and an answer survives a filter that hides the choice naming it, because what someone chose is not a function of what the list is currently showing. The DOM half is C5's: one renderer is registered for both types. The combobox UI is deferred with C5 |
| C7 | boolean (switch + radio render modes, labelTrue/False, valueTrue/False) | ☑ | `parity/C7-boolean` (unit), `parity/C7-boolean-switch` and `parity/C7-boolean-radio-with-custom-values` (demo). Unanswered is a third state, not `false`: "was never asked" and "did not agree" are different facts and only the model can keep them apart |
| C8 | rating (numeric, stars, smileys, rateValues, min/max descriptions, display mode auto-switch) | ☑ | `parity/C8-rating` (unit), `parity/C8-rating-stars` and `parity/C8-rating-auto-collapses-a-long-scale` (demo). Stars and smileys are `aria-hidden` decoration over real radios, so the scale announces "4" rather than "star star star star"; `auto` collapses at eleven steps, and the collapsed form is a labelled control rather than a group so the select has a name of its own |
| C9 | ranking (drag reorder, selectToRankEnabled, keyboard support) | ☑ | `parity/C9-ranking`, `parity/C9-select-to-rank`, `parity/C9-reorder-primitive` (unit); `parity/C9-keyboard-reorder` (browser, in real Chromium); `parity/C9-drag-reorder`, `parity/C9-keyboard-reorder`, `parity/C9-select-to-rank` (demo, with a real pointer drag). Built as a **reusable interaction** rather than as ranking code, per [ADR-0009](./adr/0009-creator-drag-and-drop.md) constraint 3: `moveWithin` is the list arithmetic in core, `useReorder` the pointer-and-keyboard adapter in `@kajay/react`, and neither knows what is being reordered — Phase 3's Creator extends this instead of introducing a second drag system. Pointer events throughout, never HTML5 drag-and-drop, which has no keyboard story. The model is updated *during* a drag, so what is on screen mid-gesture is the answer as recorded rather than a preview that could disagree with it. Space or Enter picks a row up, the arrows move it, Escape puts it back, and every step is announced with its position and the total — a row sliding past its neighbours is otherwise entirely invisible. Mutation proved the arrow keys must be claimed (an unclaimed arrow scrolls the page out from under the respondent) and disproved an explicit refocus, which changed nothing because React already moves the focused element when rows are keyed by identity. An untouched ranking records **no answer**: the presented order is the author's, and banking it as agreement would put an opinion in the data that nobody expressed. **Unproven guard:** the pointer-id check that stops a second finger ending someone else's drag — there is no multi-touch harness here to demonstrate it |
| C10 | imagepicker (single/multi, imageFit, contentMode image/video) | ☑ | `parity/C10-imagepicker` (unit + demo). The one select type whose **arity is a property** rather than its type — everywhere else the split is a design-time fact, which is why those semantics live in two base classes. `applySelection` is the single point where the switch happens; a `select` override alongside it changed nothing, which a mutation test proved by surviving its removal. The choice text is every tile's accessible name whether or not `showLabel` draws it: a grid of unlabelled pictures is not an answerable question for anyone who cannot see them |
| C11 | multipletext (items, itemSize, per-item validation) | ☑ | `parity/C11-multipletext`, `parity/C11-multipletext-per-item-validation` (unit + demo). The answer is one object under the question's name, so `{address.city}` reaches a single field with nothing added — the path resolver already walks dotted references. Errors carry a `path` naming the field that earned them, which is what lets a renderer put the message beside the input instead of prefixing every line with a label the field already shows; §F's matrix cells want the same — and now use it. **Two defects surfaced while building F1**, both fixed here: an untouched multipletext reported none of its required items (the question was empty, so the check stopped before reaching them), and a message reported against the question as a whole had no slot to render into. Both are pinned by named tests |
| C12 | html (display), image (display), expression (read-only computed with displayStyle/format) | ☑ | `parity/C12-display-elements`, `parity/C12-expression-question` (unit); `parity/C12-html-and-image`, `parity/C12-expression-question` (demo). `html` and `image` descend from a new `displayelement` base rather than from `question`, so the Creator's generated property grid never offers `isRequired` on a paragraph of text. **Markup is rendered as markup** — that is the feature — so a definition is code at the host's level of trust; a host taking definitions from untrusted authors passes `sanitizeHtml` to `<Survey>`, and none ships here because a sanitizer that is nearly right is worse than none. `expression` formats through a fixed `en-US` locale so the same definition reads the same everywhere; making that follow the survey locale is §J |

## §D — Validation

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| D1 | Required with custom `requiredErrorText`; `isRequired` on any type incl. matrix rows | ☑ | `parity/D1-required` (demo + browser + unit) for every question type; `parity/F1-matrix-required` (unit) for matrix rows, where `isAllRowRequired` demands each visible row and the authored `requiredErrorText` applies to the row, because the row is what asked for an answer. `requiredIf` reaches the same gate as `isRequired`. Closing this found a real defect in the half that was already green: a **composite question whose answer was entirely empty reported nothing at all**, so a required multipletext field said nothing until some *other* field was filled in. The empty-answer check now asks the question whether it has parts of its own to speak for (`checksEmptyAnswer`), and those messages **replace** the question-level one rather than joining it — the respondent is told where to act, not merely that something is missing. Proven by `parity/D1-required-part-of-an-untouched-question` (demo) and the C11 unit tests. A question-level message on a composite question also had nowhere to render, and now has a slot
| D2 | Built-in validators: numeric (min/max), text (min/max length, allowDigits), regex, email, expression, answercount | ☑ | `parity/D2-built-in-validators`, `parity/D2-expression-validator` (unit); `parity/D2-email-validator` (demo); `parity/D2-validators` (browser) |
| D3 | Custom validators + async validators | ☑ | `parity/D3-async-validators` (unit + browser + demo). The demo registers its own `AsyncValidator` subclass exactly as a host would — nothing about it ships in the library |
| D4 | Server-side seam: `onValidateQuestion` / a host server validator, with async completion | ☑ | `parity/D4-server-validation`, `parity/D4-validate-question-event` (unit); `parity/D4-server-validation` (browser + demo). The server hook is a promise, not SurveyJS's `complete()` callback — a callback the host must remember to invoke is a hung survey waiting to happen. **Every path out of an outstanding check puts `isValidating` down**, including the ones nobody planned for: a hook that resolves with something which is not a list of errors (which fails past the guards that catch one that *rejects*), a host rule that throws while the reply is being applied, and a reply discarded as stale. All three are proven, the last after a mutation showed nothing asserted the flag came down when a respondent navigated away mid-check. Whatever the cause, it is reported as a *check* failure and never as an objection to an answer — the alternative is a Next button reading "Checking…" for the rest of the survey |
| D5 | Error placement (top/bottom), page-level vs question-level validation, validate on value change vs on next page | ☑ | `parity/D5-validation-scope`, `parity/D5-check-errors-mode`, `parity/D5-error-location` (demo + unit + browser); `parity/D5-errors-announced` (unit) |
| D6 | `validationEnabled` toggle; focus first invalid question | ☑ | `parity/D6-validation-enabled`, `parity/D6-focus-first-error` (demo + browser); `parity/D6-first-error-question` (unit) |

## §E — Navigation, flow & state

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| E1 | Pages, panels (nested, collapsible, visibleIf), elements ordering | ☑ | `parity/E1-panels`, `parity/E1-panel-visibility`, `parity/E1-panel-enable`, `parity/E1-panel-collapse` (unit, browser, host E2E). Panels nest arbitrarily and round-trip. A page is deliberately **not** a page element — parenting it under the same base would have made a page a legal child of a page, which the contract caught. Collapsing lives on the model, not in React: the definition declares the starting state and the Creator has to edit it. Only a panel that authored `state` is collapsible; one left at the default is a grouping device, and a toggle would invite hiding content the author never meant to hide |
| E2 | Prev/next/complete flow; page visibility recalculation; questionsOnPageMode (standard, singlePage, questionPerPage) | ☑ | `parity/E2-navigation`, `parity/E2-page-visibility`, `parity/E2-questions-on-page-mode` (unit, browser, host E2E). Every index counts **visible** pages, so a hidden page is not somewhere a respondent can stand. Hiding the page underneath a respondent clamps to the last visible one and announces the move, rather than leaving the renderer drawing nothing. `questionsOnPageMode` reshapes what navigation and the renderer see while leaving the authored `pages` untouched, and the reshaped list is memoised on its structure so React does not remount every question per render |
| E3 | Progress bar (pages, questions, correct-answer variants) + TOC navigation | ☑ | `parity/E3-progress`, `parity/E3-progress-correct` (unit), `parity/E3-progress`, `parity/E3-progress-correct` (browser), `parity/E3-progress-and-toc` (demo). `showProgressBar` (off/top/bottom/both), `progressBarType` (pages/questions/requiredQuestions/**correctQuestions**) and `showTOC`. Measurement is a **pure function over the survey** exported from core, so the renderer gained no model surface and the arithmetic is testable without a DOM. `pages` counts what is **behind** the respondent — the page they are looking at is not finished, and the flattering alternative tells someone they are done with a page of questions still in front of them. Unreachable questions are not in the total, or progress would fall as branches opened. Nothing to answer counts as nothing outstanding, because an empty bar on a form that cannot be advanced is the more misleading lie. A native `<progress>` with a text label, since "two thirds" is a shape and "2 of 3 pages completed" is the fact that survives being read aloud. The TOC is a labelled `<nav>` of buttons with `aria-current`, and jumping **does not run the validation gate** — a contents list is for looking around, and refusing to let someone glance at page four because page two is incomplete makes it useless exactly when it is most wanted. The correct-answer variant closed with E8: it reads `scoreQuiz`, counts **marks** rather than questions, says "1 of 2 correct" rather than "completed" — these are earned, not done — and is the only one of the four that can go *down*. Closing it found a defect in the other three: **nothing subscribed the bar to answers**, so a question-counting bar held its first reading until a page turn or a rule forced a render. The subscription sits on the bar, not the form, or one number would re-render every question on the page per keystroke |
| E4 | Preview mode before complete (showPreviewBeforeComplete, edit-from-preview) | ☑ | `parity/E4-preview` (unit + browser + demo). Preview is a **survey state**, not a flag beside the others, and that is what makes it read-only by construction: the survey reports itself read-only while previewing, so every question already is and no renderer is told to make it so — a mutation removing that one clause is killed by the model, not by the DOM. It draws the real question renderers rather than a summary table: an answer shown in a different control from the one it was given in invites a misreading, and a table needs its own formatting per question type, which is where the table and the form start to disagree. The last page's validation gate runs **before** the preview, so nobody reviews answers the survey is about to refuse. Edit returns to the page they left rather than the first one — a preview that costs a respondent their place is one they learn not to open. `showAnsweredQuestions` leaves out what nobody touched, and unreachable questions are never reviewed because they were never asked. A `complete` trigger still ends the survey outright: it says complete, not "ask them again" |
| E5 | Completed page (completedHtml, completedHtmlOnCondition), loading/empty states | ☑ | `parity/E5-survey-state`, `parity/E5-completed-html` (unit + browser); `parity/E5-completion-flow`, `parity/E5-completed-html-on-condition` (demo). What the survey is doing is **one value**, not a handful of flags — loading, empty, running, completed — because a renderer has to draw exactly one thing and two booleans let it be asked to draw a completed page for a survey that is still loading. Loading outranks everything (while the host says the model is arriving, nothing else it says is settled) and E4's preview is a state, not a flag beside them. `completedHtmlOnCondition` is an ordered list and the first match wins; a broken condition selects nothing rather than everything, because the wrong ending is worse than the default one. **Placeholders are escaped before they reach the markup**: the template is the author's and is meant to be markup, the value is the respondent's and is never trusted — unescaped, this is stored XSS on every survey that greets someone by name, and a mutation proves both halves. The authored markup round-trips whatever the conditions currently say. Default wording lives in the renderer, not the model, so it never reaches a definition; §J moves it to the string dictionary |
| E6 | Partial save/resume: serialize `data` + currentPageNo; onValueChanged/onCurrentPageChanged seams; sendResultOnPageNext pattern | ☑ | `parity/E6-save-and-resume`, `parity/E6-save-seams` (unit); `parity/E6-save-and-resume` (demo, through a **real page reload**). `survey.progress` is a plain-JSON snapshot and `survey.restore` applies it. **The page is remembered by name, not by index** — a deliberate departure from the row's own wording: add a page, reorder two, or let different answers make different pages visible, and index 2 is somewhere else, so a resumed respondent lands on a page they have never seen with nothing to say anything went wrong. A name either still exists or does not, and a page that no longer does leaves them at the start rather than nowhere. Answers restore **before** the page, because page visibility is computed from them — navigating first aims at a page that has not appeared yet, which a mutation reversing the order duly proves. Calculated values are recomputed rather than stored: writing one back would put a value in the response that nothing computed. The two save seams are the existing events, and what a host reads on `onCurrentPageChanged` already names the page they arrived at. The demo stores to `sessionStorage` and clears on completion — resuming into a survey already submitted invites a second submission — and treats a stored snapshot as untrusted input, since it may predate the definition it is being applied to |
| E7 | Read-only / display mode for the whole survey and per question | ☑ | `parity/E7-read-only` (unit + browser + demo), `parity/E7-axe: every question type in read-only` (demo). `question.isReadOnly` combines the survey-wide flag and the question's own, in the model — leaving that to each adapter is how one renderer honours the survey flag and another only the per-question one. It reaches the question through the **value host** seam, so `Question` still never imports `Survey`. **Read-only is not disabled**: the value keeps its place in the tab order, its focus and its announcement, which is exactly what someone reviewing what they submitted needs — `disabled` would take all three away. Native `readonly` where HTML defines it; `aria-readonly` **on a role that allows it** plus a refused change where HTML has none. **That part shipped wrong, in every place it was done.** `aria-readonly` is not defined on `group`, which is what a `<fieldset>` maps to, and a single `readOnlyGroup()` helper spread it onto whatever it was handed — so five renderers emitted markup axe calls `aria-allowed-attr`. K3's sweep caught the choice question; the other four went in the checklist as "unproven rather than known-good", which was the honest state at the time and not a design decision, however the row then read. They are now **proven wrong and fixed**. Nothing had reached them because whole-survey read-only was not reachable in the demo at all, so the fix began with a control that reaches it and a sweep over every question type in that state; it failed, and named nine elements across five renderers — one of them `paneldynamic`, which was not in the brief, because `MatrixFrame` is shared by four question types. The blanket helper is gone, replaced by three whose **names state what they assume about the element they are spread onto**: `readOnlyRadioGroup` supplies `role="radiogroup"` *with* the attribute, because the attribute is legal only because of the role and handing them out separately is how the two come apart again; `readOnlyControl` is for a role that already carries it — checkbox, switch, `<select>`, text field; `readOnlyAction` emits `aria-disabled` for a `radio` or a button, where ARIA offers no read-only state at all. There is deliberately **no helper for a plain `group`**: the honest answer is that the controls inside it say it instead, which is what a read-only matrix now does per radio and a read-only ranking per row. `aria-disabled` is not a retreat to `disabled` — the control keeps its tab position, stays readable and stays announced; what it gives up is the claim that it can be operated, which for a read-only answer is true. It does have a cost worth knowing: Playwright and vitest-browser treat `aria-disabled` as failing the "enabled" actionability check, so a test that wants to prove the click is refused has to dispatch it raw. Six mutants die here, including the original defect reinstated verbatim on the matrix's `<fieldset>`, which the sweep catches. A read-only `<select>` offers only what was chosen, which is genuinely unchangeable rather than merely refusing. The refusal is on the **change handler, not the click**: cancelling a click's default stops the browser toggling a checkbox but not React reporting the change, which cost a failing test to discover. Programmatic writes still land, and that is the point — the demo's reference field is one nobody may type into and the engine fills in. **Not in scope for this row:** `readOnly` on a panel or page, which needs the container walk §F's composites will want anyway |
| E8 | Quiz mode: maxTimeToFinish, per-page timers, correctAnswer, quiz scoring, showTimerPanel | ☑ | `parity/E8-correct-answers`, `parity/E8-partial-credit`, `parity/E8-quiz-placeholders`, `parity/E8-timer`, `parity/E8-page-timer`, `parity/E8-timer-panel` (unit); `parity/E8-timer-panel` (browser, real Chromium with an injected clock and a faked interval); `parity/E8-timer-panel`, `parity/E8-quiz-scoring` (demo); `survey-timer-completes` and `page-timer-turns-then-completes` ([conformance v1](../conformance/v1/lifecycle.json)). **`correctAnswer` is an ordinary property on an ordinary question** — there is no quiz question type — and whether a question is graded asks whether it was *authored*, not whether it is truthy, or a true/false question whose answer is `false` drops off the paper. Scoring is a pure function over the survey, like `measureProgress`, and grades only **reachable** questions: a branch nobody was shown must not cost marks. A multi-select is worth a mark per expected choice **less one per wrong tick, floored at zero** — a deliberate divergence, because counting only matches makes "tick everything" the winning strategy and scoring per offered choice hands most of the marks to someone who answered nothing. `{correctAnswers}` and `{quizQuestionCount}` report the score on the completed page, in marks on both sides so the fraction is true; an answer of the same name still wins, since silently replacing a respondent's data is the surprise hardest to diagnose. **Core owns no interval.** The model computes what the clocks read from the injected `now` and acts when one runs out; the host ticks it — the renderer once a second, a test as fast as it likes — so no timed test is a race and the conformance corpus can move time by hand. Time passes whether or not anyone ticks: backgrounding the tab does not pause the exam. Nothing is timed until the host **starts** it, because a survey parsed on a server to score a stored response is not being sat by anybody. Expiry runs **no validation**: a page whose time is up must be leavable with an answer missing, and a survey out of time is handed in as it stands. The survey's limit beats the page's, or a respondent lands on a fresh page of a survey already over. A page limit **stops applying outside the running state** — found by a surviving mutant: without it, a last page that timed out into the preview submitted itself out from under somebody still reading. Expiry emits no event of its own; it reaches listeners as the completion or navigation it causes. The timer panel counts **down** where there is a limit and is deliberately not a live region — announcing a number that changes every second interrupts a screen reader for the length of the survey. **Not built:** correct answers ship to the browser in the definition and the clock is the respondent's, so this is a self-check quiz, not a proctored exam; anything that matters must be graded again server-side. Scoring has no conformance case — v1 has four adapter operations and none can ask a pure query, and adding a fifth would un-conform every adapter that passes today ([why](../conformance/v1/README.md)) |
| E9 | Clear invisible values policies (onHidden, onComplete, none) | ☑ | `parity/E9-clear-invisible-values`, `parity/E9-reachability` (unit) for all three policies; `parity/E9-clear-invisible-values` (demo) for `onHidden`, the one you can watch happen. **Reachability, not the question's own visibility**: a question inside a hidden panel or on a hidden page is out of reach however visible it is itself, and a mutation swapping the two is killed by exactly that case. `onHidden` clears **inside the settle** that hid the question, so the first thing anyone hears about the change already has the answer gone — announcing a model where the question is unreachable and its answer is still in the data is what ADR-0004 exists to prevent. The sweep repeats until nothing more falls out, because clearing an answer can hide the question after it. Two authoring mistakes are survivable rather than fatal: a rule that refills what the policy clears is bounded by a pass cap, and one that *reads* the answer it refills — a stack of clears rather than a loop — by a re-entrancy guard on the after-settle hook. `onComplete` is the default and runs before `onComplete` fires, so the event never carries the data the policy exists to withhold. Disabled is not invisible: an `enableIf` chain leaves the answer alone, which the demo pins down |
| E10 | First-question autofocus, checkErrorsMode, goNextPageAutomatic | ☑ | `parity/E10-go-next-page-automatic` (unit), `parity/E10-autofocus` (browser); `checkErrorsMode` was built with D5 and is proven by `parity/D5-check-errors-mode` (unit + demo). Auto-advance applies only to questions a **single action finishes**, which the question type states about itself rather than a list kept elsewhere — a text answer arrives a character at a time, and a survey that turned the page after the first letter of a name would be unusable. It goes through the ordinary gate, so saving a click never skips a check, and unreachable questions do not hold the page back. **The last page never turns itself in**: submitting is a decision, and a survey that submitted itself on the final click would take it from the respondent along with any chance to look back. Autofocus is off by default and stays off — on a long page it drops a screen-reader user past the page title and any instructions above the first field, so doing it unasked is a bad default rather than a helpful one; both halves of that are tested. A trigger that writes the last one-step answer can also move the page, which is a caveat rather than a design: the model cannot tell a respondent's write from a rule's |

## §F — Matrix family

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| F1 | matrix (single-choice rows): columns/rows, isAllRowRequired, eachRowUnique, alternate rows | ☑ | `parity/F1-matrix`, `parity/F1-matrix-required`, `parity/F1-matrix-each-row-unique`, `parity/F1-matrix-definition` (unit); `parity/F1-matrix` (browser); `parity/F1-matrix`, `parity/F1-each-row-unique`, `parity/F1-alternate-rows` (demo). Rows and columns are **`itemvalue` collections**, not a shape of their own: a matrix row is a choice in every respect that matters — a value, display text, and a `visibleIf` the same engine evaluates — and a parallel type would have meant a second definition of the same thing for the registry, the Creator and every host to learn. That took generalizing choice conditions into `conditionalItems`, which each question type now answers for itself rather than being named in a list somewhere else. The answer is **one object keyed by row value**, exactly as a multipletext keys by field, so `{comparison.docs}` reaches a single row with nothing added and an emptied matrix collapses to `undefined` rather than lingering as a `{}` that no emptiness test would catch. Errors are reported **per row**: at most one per row and in row order, because a matrix with eight rows and one message at the top says that something is wrong and not where. `eachRowUnique` objects against the *second* row to use a column — the first is not the mistake, and marking it would send the respondent back to an answer they had just made. A hidden row is never asked for an answer nobody can give. In the DOM it is a real table: a cell is named by its row header **and** its column header, so it announces the question it is asking rather than its position in a grid — and a first attempt that put the error message inside the row header quietly pulled that text into every cell's accessible name, which a browser test caught. `alternateRows` puts a class in the markup and nothing else: the library ships no stylesheet, and shading is the host's decision (§I). **Gap noted:** a column that becomes invisible leaves any answer pointing at it in place — clearing it is E9's rule applied to a part of an answer, and belongs with the cell model in F2
| F2 | `matrixcells` — per-column question type (dropdown, checkbox, radiogroup, text, comment, boolean, expression, rating), column totals, cell visibleIf/enableIf with row context | ☑ | `parity/F2-matrix-cells`, `parity/F2-cell-conditions`, `parity/F2-column-totals`, `parity/F2-definition` (unit); `parity/F2-matrix-cells`, `parity/F2-cell-conditions`, `parity/F2-column-totals` (browser); `parity/F2-matrix-cells` (demo). **A column is a question, and a cell is an instance of it.** SurveyJS's `cellType` is simply `type` here — our format already has a word for what kind of thing something is — so a column is an ordinary question definition and each cell is a real question built from it by a registry-driven copy. That is the whole design: a `rating` in a table is the same model, the same validators and the same *renderer* as a `rating` on a page, including a host's own replacement for it, which a browser test pins down. The alternative — a bespoke cell object with a `cellType` switch — is a second, quietly diverging implementation of every question type. Cells are pointed at their slot through a value host, exactly as a page question is pointed at the survey, so nothing in `TextQuestion` knows a matrix exists. **Row context is resolved by rewriting, not by an evaluation scope**: `{row.price}` on the column becomes `{basket.docs.price}` on the cell, so the dependency graph sees statically what a cell condition reads and re-evaluates it in the same settle — an overlay in the evaluator would have hidden that from the graph. The definition keeps what the author wrote; only the cell carries the rewritten form. **Totals are declared on the matrix, naming the column** (`totals: [{ column, kind, format }]`), not on the column as SurveyJS does: our columns are questions, and a question has no business declaring a table footer — putting it there means a matrix-only property on every text field in every property grid forever. A column nobody answered totals to *nothing* rather than zero. Errors are reported twice on purpose — on the cell, where its own renderer draws them, and on the matrix with a `row.column` path, so the survey knows the table is unanswered. Building this found two defects invisible from the model: every cell of a column shared a DOM id (a browser points every label at the first match, so typing in row two wrote into row one), and every renderer read its value from `survey.getValue(name)` rather than from the question — which for a cell is nothing at all, and only looked right because React skips a DOM write it thinks is a no-op. Both are fixed with a named test each. **Not in scope here:** detail panels (F4) and `{row.col}` in *totals* expressions (F5)
| F3 | `matrixdynamic`: add/remove rows, min/max rows, confirmDelete, defaultRowValue, copy default from last row | ☑ | `parity/F3-matrix-dynamic`, `parity/F3-row-limits`, `parity/F3-row-defaults`, `parity/F3-dynamic-validation`, `parity/F3-definition` (unit); `parity/F3-matrix-dynamic`, `parity/F3-confirm-delete`, `parity/F3-row-limits` (browser); `parity/F3-matrix-dynamic` (demo). The same cells as F2, from the same base class; what differs is that **the rows are the answer** — an array of row objects — so the row count survives a save and resume with nothing stored beside it. A count kept anywhere else would be a second source of truth that disagreed the first time a host wrote `data`. An added row stays as `{}` even before anything is typed into it, because a respondent who pressed Add created something; rows below `minRowCount` are *implied* rather than stored, so an untouched matrix is still no answer at all rather than a page of blanks in the response. A new row is a **copy** of `defaultRowValue`, never the object itself: `data` goes to the host, and handing out the definition means a host tidying its results edits the survey it was given. A mutation proved that guard was unobservable until a test asked the question directly. `defaultValueFromLastRow` wins over `defaultRowValue` — it is the more specific statement. **`confirmDelete` is answered in the page, not with `confirm()`**: a native dialog cannot be styled or translated and is the control a respondent using a screen reader is most likely to lose their place in. **Known non-guard:** rebuilding cells after a *removal* is hygiene, not correctness — cells read their row live by index, so the answers move with them; what it prevents is graph rules accumulating for rows that no longer exist. The mutation removing it survives and the code says why rather than a test pretending otherwise
| F4 | Detail panels (expandable row detail with its own elements) | ☑ | `parity/F4-detail-panels` (unit + browser + demo). A detail element is a **cell like any other** — same builder, same row-scoped value host, same validation pass — drawn under the row instead of in a column, so a detail answer lands in the same row record and `{row.amount}` works in it unchanged. A table runs out of horizontal room long before a form runs out of questions, and this is where the long ones go. `underRowSingle` keeps one row open at a time. **A detail holding something invalid opens itself**: the respondent has just been told the page is wrong, and the field at fault was behind a closed panel — an error nobody can see is one nobody can fix. Which row is open is state the *model* holds, so another adapter can ask and the error rule has somewhere to live, but it is **not** announced on the event channel that carries answers: nothing in the survey depends on it, and the component that opened it is the one that re-renders. Expansion never serializes, for the same reason computed visibility never does
| F5 | Row/column expressions: `{row.col}` references, totals row with expressions | ☑ | `parity/F5-row-expressions`, `parity/F5-total-expressions`, `parity/F5-scope-rewriting` (unit); `parity/F5-row-and-total-expressions` (demo). **Every** expression a column carries is rewritten into the row's scope, not just its conditions: a computed `expression` cell, a `defaultValueExpression`, a `setValueIf`, and the expression of a validator hanging off the column. Which properties those are is now the **registry's** answer — properties declare `isExpression` — so one added later is covered by declaring itself rather than by being remembered in the cell builder, and §M's logic editor gets the same list for free. A cell's rules are ordinary graph rules with the cell's path as their declared write, so a computed cell settles in the *same transaction* as the answer that drives it rather than a render later. **Totals may be expressions**, computed in declaration order, where `{row.price}` means that column's own total — the same scope word one level up, because in both places `row` means "the record this belongs to". A total naming one declared after it sees nothing, which is the rule a spreadsheet applies and cheaper to explain than a second dependency graph for four numbers. A total that is broken, or that evaluates to something which is not a number, shows **nothing**: a figure under a column is read as a fact and there is no way to caveat one in a table cell. The row scope inside a total is an evaluator overlay rather than a rewrite, because a column total is not an answer and never will be — the one place where the honest answer is a local scope, and it is confined to a value computed on read that no rule depends on
| F6 | Responsive/mobile rendering mode for matrices | ☑ | `parity/F6-mobile-mode` (browser, for the explicit modes; demo **at a 375px viewport**, for `auto`). `mobileMode` is auto/table/list on all three matrix types, which are not related by inheritance — a phone is the same problem for each, and an author who learned the word on one should not find it spelled differently on the next. `auto` resolves against a **real media query**, subscribed rather than read once, so a resize and a rotation both land. That is the whole reason this row is not just a class name: the library ships no stylesheet, so a hook a theme might use would be a feature nobody could see, and a test asserting the hook would pass just as happily if it were wired to nothing. The breakpoint is in `rem`, so a reader who has turned text up runs out of room at the width they actually run out of room. The list layout is a **structural** change: each row becomes its own group with the row title as its legend, and the cells keep the labels they already have — which is why a cell is titled for its row *and* its column, a title that reads correctly under a legend and behind a column header alike. A single-select matrix gets the same treatment and needs it most: five columns of radio buttons in 375 pixels is a row of unlabelled dots. On a server there is no screen to ask, so `auto` renders the table — the layout that degrades to a scroll rather than to the wrong thing. **The browser suite now states a desktop viewport**: making layout depend on the window meant three table tests silently became list tests

## §G — Dynamic panels

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| G1 | paneldynamic: template panel, add/remove, min/max panels, panelCount binding | ☑ | `parity/G1-paneldynamic` (unit + browser + demo). **The same machinery as a dynamic matrix**, which is the whole result: a matrix row and a panel instance are both a record in an array holding instances of template questions bound to a local scope, so §F's builder, value host, scope rewriting and validation pass were extracted to `RepeatingQuestion` and this row is what is left over — the drawing. The instances *are* the answer, so how many there are survives a save and resume with nothing stored beside it; instances below `minPanelCount` are implied rather than stored, so an untouched panel is still no answer at all. Removing asks first, in the page, with more force than a matrix row: a panel can hold a page of typing
| G2 | Render modes: list, tab-per-panel/progress navigation modes | ☑ | `parity/G2-render-modes` (unit + browser). `list` draws every instance; `tab` gives a button per instance and `progress` a pair of arrows with a position — the same model state under two presentations, because which an author picks is a question about *how many* instances there will be rather than about what moving means. **Adding moves to what was added**: a control that creates something out of sight appears to do nothing. The position is clamped on **read**, not only where instances are removed — a host restoring a shorter response moves the ground under a stored index, and a mutation showed the write-side clamp hid that until a test wrote `data` directly
| G3 | Template expressions with `{panel.q}` scoping; valueName data binding/shared data | ☑ | `parity/G3-panel-scope`, `parity/G3-value-name` (unit); `parity/G3-panel-scope` (browser + demo). `{panel.q}` is F5's `{row.col}` under the word that reads better here — both mean "this record", and the scope word is the only thing the two types differ by. It is resolved the same way: rewritten into a real path (`{people[1].age}`) when the instance is built, so the dependency graph sees statically what a template condition reads. `valueName` is the data-binding half: a question's answer key, when it should differ from its name. Two questions sharing one share an answer — the same thing asked on two paths comes back as one field. Identity stays with `name`, because two questions that share an answer are still two questions; what follows the *answer* — clearing an unreachable one, re-checking a changed one — follows `valueKey`
| G4 | Nested composites: panels in dynamic panels, matrices in panels | ☑ | `parity/G4-nested-composites` (unit + browser + demo). Mostly the absence of a restriction: `templateElements` admits **page elements**, so a panel in a template is a panel and a matrix in one is a matrix, each drawn by whatever draws it anywhere else. Two things it did need. A repeating question created at runtime is handed the registry its own instances are built from — `parseSurvey` can only reach the questions on a page, and this one was created long afterwards. And conditions are registered for every *element* of an instance rather than every question, because a group carries a `visibleIf` and is not a question; without that a nested group was permanently visible. **A real defect surfaced in the demo E2E**: only the top element of an instance was being titled and given a unique key, so a question two levels down shared both with its twin in the next instance — the §F cell-id defect one level deeper, and the unit tests had missed it

## §H — Media & upload

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| H1 | file: single/multiple, drag-drop, accepted types, max size, previews, storeDataAsText vs onUploadFiles seam, camera capture option | ☑ | `parity/H1-file`, `parity/H1-file-rules`, `parity/H1-upload-seam` (unit); `parity/H1-file` (browser + demo). **Core never sees a DOM `File`.** The model's idea of a file is a name, a type, a size and where the content is; the adapter reads the real one and hands that over. That is what keeps core DOM-free with a feature whose whole subject is a browser API. The answer is **always an array**, even for a single file: a question that changed the shape of its answer when `allowMultiple` flipped would break every host reading it. `acceptedTypes` and `maxSize` are **model rules**, not picker hints — a respondent can drag a file straight past `accept`, and nothing in HTML enforces a size at all — while the same string is still handed to the picker as the affordance, the pairing C1 established for `min`. `storeDataAsText` versus an uploader is one decision taken once: either the content travels in the response or the host stores it and the response carries a reference. With neither, the file's *description* is kept rather than nothing, so a host that has not wired an uploader yet sees what happened. **A failed upload changes no answer** and says why: recording a reference to something nobody stored is the one outcome that looks like success to everybody downstream. Dropping is an addition to a real `input[type=file]`, never a replacement — the input is what a keyboard reaches and what a phone turns into a camera
| H2 | signaturepad: draw, clear, penColor, background, output format | ☑ | `parity/H2-signature` (unit + browser + demo). The answer is a data URL, so it travels like any other string and needs no seam of its own. Pointer events rather than mouse and touch separately, with pointer capture so a stroke that wanders off the canvas keeps drawing. The pad is **repainted from the answer** after every re-render: the answer is the truth about what was signed and the canvas is a view React will happily hand back empty — without it the next stroke would save a signature missing everything drawn before. A mutation found that nothing proved it. Signing and then erasing is **not an answer**, so a required signature still demands one. **Named gap:** a canvas cannot be operated by keyboard. §J4 owns it and the answer is an alternative way to sign rather than a better canvas; until then the control at least announces whether it has been signed instead of being an unlabelled box
| H3 | Download/clear seams for stored files (onDownloadFile, onClearFiles) | ☑ | `parity/H3-file-seams` (unit + demo). Both are injected beside `fetchJson`, for the same reason: only the host knows where a file went. `downloadFile` mints a URL for a stored file — a signed one that expires cannot be kept in the answer — and its absence falls back to whatever the entry already carries, so a file with its own content or URL needs nobody's help. `clearFiles` is told when files leave an answer. It is **housekeeping, not a veto**: the answer changes whether or not the host can delete its copy, because refusing to let a respondent detach a file would make it theirs. A rejection there is swallowed rather than becoming an unhandled one

## §I — Theming & appearance

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| I1 | CSS-variable design tokens covering all components; documented | ☑ | `parity/I1-tokens` (demo, against the stylesheet the library actually ships) and [the token contract](./design-tokens.md). Every component now has its styles **in `@kajay/themes`**, including the matrix family, repeating panels, files and signatures — all of which had been styled on loan in the host demo, each with a comment admitting it. That loan is repaid: the demo's stylesheet is now its own chrome and nothing else. `--kajay-spacing` is the only spacing value in the sheet; a rule wanting half a gap says `calc(var(--kajay-spacing) / 2)` rather than `8px`, so one token rescales the whole survey rather than most of it. **Colour is never the only cue** — an invalid field carries `aria-invalid` and a message in words, a selected tab says `aria-selected` — so no token change can make the survey unreadable to someone who does not perceive the difference
| I2 | Theme JSON format (palette, panelless/panel modes, sizes, corner radius, background image/opacity) applied at runtime | ☑ | `parity/I2-theme-format` (unit), `parity/I2-theme` (browser + demo). A theme is **plain JSON that maps onto CSS custom properties**, and `themeVariables` is a pure function from one to the other — so a theme can be computed on a server, stored, diffed in a test or handed to a renderer and mean the same thing every time. The renderer's whole part is putting that map on one element and letting it inherit, which is why **`@kajay/react` never imports `@kajay/themes`** (the architecture check forbids it) and why two surveys on one page can be themed differently. **What a theme does not name, it does not set**: an absent field leaves the stylesheet's default alone rather than overwriting it with a guess, which is what stops a partial theme becoming a broken one. `panelless` is expressed as two lengths rather than a mode flag, so no rule branches on it. A backdrop is its own layer, so its opacity applies to the picture and not to the text in front of it. `variables` is the escape hatch and wins over everything computed
| I3 | Preset themes incl. light/dark in `@kajay/themes` | ☑ | `parity/I3-presets` (unit + demo). Light, dark and panelless, each naming a **full** palette — a preset that left a colour out would inherit whatever the last theme set, which is exactly how switching themes leaves one wrong colour behind, and a test enumerates them to prevent it. Both delivery routes are proven: the CSS preset (`@kajay/themes/themes/dark.css`, exercised from an installed tarball by the pack test) and the runtime object, switched live in the demo
| I4 | Per-instance css class overrides (survey-level css merge) | ☑ | `parity/I4-css-overrides` (browser + demo). Classes are **added to the built-in names, never substituted for them**: replacing them would take the shipped stylesheet with it and leave a host who wanted one extra class reimplementing everything. Supplied per `<Survey>` rather than in the definition, because a class name is a fact about one host's stylesheet and not about the survey. A part nobody named is left exactly as it was, so a partial override cannot blank the rest. Every page element goes through one wrapper, which is why the host's class reaches question types the library has never heard of |
| I5 | Responsive layout: startWithNewLine, colCount, question width/minWidth, title alignment options | ☑ | `parity/I5-layout` (browser ×4 + demo). Pages and panels lay out as grids, so `colCount` reaches CSS as `--kajay-col-count` rather than as a class per count — an author may ask for any number and a stylesheet cannot enumerate them. `startWithNewLine` is a **grid start on the element that wants the break**, not a break element in the tree that would mean nothing to a screen reader. It **defaults to false**, a deliberate divergence from SurveyJS, where the true default makes `colCount` do nothing until every question on the page is edited: the layout an author asks for should be the one they get. `width` and `minWidth` are CSS lengths on the same wrapper, and `titleLocation: hidden` is **visual only** — the title is clipped but still names the input, because a field with no accessible name is unanswerable to anyone who cannot see the column header above it. All four live on one wrapper per page element, which is why none of the twenty-odd renderers had to learn a layout rule |
| I6 | Custom rendering seams: register custom renderer per question type; markdown/HTML in titles via onTextMarkdown | ☑ | `parity/A4-custom-question-renderer` (browser) proves a host-registered renderer through the public React seam, which [ADR-0019](./adr/0019-deep-runtime-modules-and-rendering-seam.md) made one dispatch table for questions, panels, display elements and custom types alike; `parity/I6-text-seam` (browser + demo) closes the other half. The text seam **returns a node, not an HTML string**. A host that wants markdown renders it themselves, with their own sanitizer, and the library never inserts markup it did not build — the same boundary `sanitizeHtml` draws for the `html` element, arrived at from the other side. It is told *which* piece of text it is being asked about, so a host can allow emphasis in a description and refuse it in a label without inspecting the string. **No markdown parser ships here**: shipping one would ship its escaping rules and its vulnerabilities with it. The demo implements the smallest thing that could be called markdown, to prove the seam rather than the syntax |

## §J — Localization & accessibility

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| J1 | Localizable strings: every user-facing property accepts `{ default, <locale> }`; survey.locale switch re-renders | ☑ | `parity/J1-localizable-strings`, `parity/J1-localizable-reach`, `parity/J1-localized-round-trip` (unit); `parity/J1-locale-switch` (browser + demo); `localized-strings-round-trip` ([conformance v1](../conformance/v1/definitions.json)). Thirty-five properties are marked `isLocalizable`, and the flag is **declared rather than inferred from the type**: `title` is prose and `visibleIf` is code, and both are `string` — an object on the second is still a type error, because translating a condition breaks the survey in whichever language somebody translated it into. **The definition stores the object as authored.** Resolving on the way in would flatten a survey to one language the first time anyone read it, and the fixed-point rule would not catch it: both passes would agree and every other translation would be gone. Resolution happens in `getStringProperty` — *one* method — which is why ninety-odd call sites read titles, placeholders and messages in the current locale without one of them learning that a title can be an object. The locale is a **shared mutable holder handed out during parsing**, not walked in afterwards: parsing is the only place that sees every element, and a walk would have to know each child collection separately and be wrong about the next one — choices, validators and multiple-text items are exactly the three it would have missed, and a test names them. Fallback is locale, then base language (`fr-CA` → `fr`), then `default`; without the middle step a French-Canadian respondent reads English in a survey that *was* translated into French. A definition's `locale` is the one it **opens** in and a runtime switch never serializes, on the same rule that keeps `visibleIf`'s current answer out. Switching rebuilds repeating instances, because a cell title is composed at build time from the template's resolved one — the only translated string the model caches, and a surviving mutant is what proved it needed throwing away. The renderer re-renders from one subscription at the survey root: a locale switch changes every string at once, so per-question listeners would be a thousand subscriptions agreeing on the same answer |
| J2 | UI string dictionaries with runtime registration; `en` complete; ≥3 seed locales proving the mechanism | ☑ | `parity/J2-string-catalogue`, `parity/J2-string-dictionary`, `parity/J2-messages-are-localized` (unit); `parity/J2-ui-strings` (browser + demo). Every string the library itself says — validation messages, navigation labels, the ranking hints, the file and timer panels — goes through a key. **`en` is complete by construction**: the catalogue carries the English text, so `EN_STRINGS` is built from it and a key can only exist by having its English written beside it. That follows ADR-0020's pattern for diagnostic codes, and it means the claim is a fact about the build rather than a line in a document; using a key that does not exist fails to compile. Three seed locales (fr, de, es), complete rather than partial — a partial seed would prove the fallback instead of the mechanism, and the fallback has its own test. They are **seeds, not shipped translations**: breadth here is a community model by design, and a deployment that cares about its French should review the file rather than trust it. **One dictionary per survey, not one per process**: the metadata registry is global by design and the suite pays for it in care, and there is no comparable reason here — a host serving two tenants must be able to word them differently. Registering merges, so overriding one string does not blank the other fifty, and a host can add a language nobody shipped (the demo adds Welsh). The last-resort fallback is the **registered** English rather than the shipped constant, which a failing test caught: a survey naming no locale at all — most of them — was silently ignoring every English override. The dictionary hangs off the same locale scope J1 already put on every element, which is what lets a validator inside a matrix cell speak the respondent's language without being handed the survey. **Not covered:** languages with more than two plural categories. English needs two forms and picks by count, and the progress labels are two keys for that reason; Polish, Russian and Arabic need a plural *rule*, which is a formatter a host supplies. Stated rather than approximated, because confidently wrong grammar is worse than an obvious gap |
| J3 | RTL support | ☑ | `parity/J3-rtl` (unit + browser + demo). **A `dir` attribute and nothing else.** The browser already mirrors layout, reorders bidirectional text and resolves logical CSS properties; a stylesheet doing any of that by hand would be a second copy of the first, wrong wherever nobody read. The whole change to `@kajay/themes` was five declarations — `text-align: left` became `start`, `padding-left` became `padding-inline-start` — because the sheet was already logical everywhere else, and a header now says why it must stay that way: a rule saying `left` is a bug in Arabic that nobody sees while reading English. **Direction is derived from the language**, not a separate flag: it is a fact about a script, and an author who translates a survey into Hebrew should not also have to remember to flip a switch — one who forgets would ship a survey laid out backwards. `textDirection` overrides for what the list cannot know: a private locale tag, or a survey deliberately laid out against its language. A hard-coded list rather than `Intl.Locale.getTextInfo`, which is not in every supported engine and would guess left-to-right on exactly the engines least likely to be tested. Scoped to the survey rather than the document, so a survey embedded in an English page may still be in Arabic and two surveys on one page may read different ways. The `dir` sits on the same wrapper the theme variables do — one element scoping everything that is true of *this* survey rather than of the page |
| J4 | Keyboard operability for every question type incl. ranking and matrices | ☑ | `parity/J4-keyboard` (demo, six scenarios). Real keys throughout: `focus()` only ever puts the cursor at the *start* of a journey, and nothing clicks its way to the state it is about to assert. The types that could have failed are the ones not built from a plain input, and each is proven separately — a **star rating** arrows between values because the glyph is `aria-hidden` decoration over a real radio (`getByLabel` finds nothing there while `getByRole('radio')` finds it, which is the distinction); a **matrix** arrows *along a row*, because each row is its own radio group and one group across the table would let a single arrow press walk into another question; a **ranking** is picked up, moved and dropped with space and the arrows, and focus stays on the row that moved so a respondent does not lose their place; a **repeating panel** adds an instance on Enter. Enter in a text field advances the page, which is the form's submit rather than anything the renderer added |
| J5 | ARIA roles/labels/error associations; axe checks pass on every demo page | ☑ | `parity/J5-axe` (demo, seven sweeps) against `wcag2a`, `wcag2aa`, `wcag21a` and `wcag21aa`: all three pages, a page showing its errors, the preview, the completed page, right-to-left, and the dark theme. Scoped to the survey — the diagnostic JSON panels around it are the demo's own scaffolding, and failing the suite for the demo's chrome would train everyone to ignore it. It found a real defect on its first run: **the shipped dark preset's primary button was white on a light accent at 2.51:1**, a control nobody with low vision could read, in the one theme least likely to be checked by someone developing in the light one. The fix is a token — `--kajay-color-on-accent` — because what sits *on* the accent does not follow from the accent: a dark theme lightens the accent so it shows against a dark surface, and that is exactly when white stops working. Contrast is a property of the palette, so the sweep runs once per shipped theme rather than once per survey. **axe is a floor, not a ceiling** — it checks names, roles, structure and contrast, and cannot check whether the survey is *operable*, which is why J4 walks the hard types with nothing but a keyboard |

## §K — Creator: design surface & toolbox

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| K1 | Toolbox listing all registered types (auto-populated from registry), search, categories, custom toolbox items | ☑ | `parity/K1-toolbox-registry`, `parity/K1-toolbox-search`, `parity/K1-toolbox-custom-items`, `parity/K1-toolbox-changes` (unit); `parity/K1-toolbox` and `parity/K1-primitives` (browser); `parity/K1-toolbox` (demo, including an axe sweep and a theme check). **Nothing in `Toolbox.ts` lists a type.** The items come from `getConcreteSubclasses('pageelement')` — not `question`, because a panel and an image are things a designer drops on a page and are not questions — and `builtInToolbox.ts` says only what they are *called* and which drawer they are in. A type that table has never heard of still appears, under a fallback title in "Other": auto-population is not a courtesy extended to types somebody remembered to list, and a test registers one to prove it. Titles and categories live in the Creator rather than on the class descriptor, because "which drawer" is a decision about a designer and the registry is the runtime's — a survey being answered on a server has no drawers. **`name` is the identity, not `type`**, so one type can appear more than once: "Rating (stars)" and "Rating (smileys)" create the same type and differ only in the `defaults` they start it with, which is also exactly the shape a host adding a custom item reaches for. Adding an existing name *replaces* it, because curation is the same act as addition — and a retitled item keeps the keywords it had, or renaming the dropdown would silently stop "select" from finding it. Search covers the title, the type (a definition author knows `paneldynamic`, not "Repeating panel") and the keywords, and a category left empty is not drawn. The model is **stateful** because the search term is designer state ([ADR-0021](./adr/0021-creator-composition.md)) and the panel holds none; the panel reads a version counter rather than the categories, since `categories` builds a fresh array per read. Picking is **reported, never performed** — where an item lands is K2 and K3, and a toolbox that reached into a survey would be the second thing deciding it. This is also the first use of the primitive seam ([ADR-0022](./adr/0022-design-system-primitives.md)): the panel draws through `Button` and `Input`, a host may replace either, an explicitly `undefined` entry means "use the default" rather than "draw nothing", and the map's partiality is proven by replacing one and leaving the other. The demo applies the theme variables to its own section rather than the panel taking a `theme` prop — the tokens are CSS custom properties and any element a host controls can carry them. **Amended by §N5:** an item now carries **starter content** — three choices, two matrix rows, a repeating panel's one template question — because until N5 walked the whole designer path, every drop produced an element a respondent could look at and could not answer. It sits in this table rather than in the registry for the reason the rest of the table does: it is a decision about a designer, and a survey arriving from a server must not grow three choices for having left `choices` out. A host's override that does not mention `defaults` keeps the built-in ones, the same rule keywords already followed |
| K2 | Drag-drop from toolbox onto surface; reorder questions/panels/pages by drag; drop indicators | ☑ | `parity/K2-slots`, `parity/K2-placement`, `parity/K2-move`, `parity/K2-placement-refusals`, `parity/K2-place` (unit); `parity/K2-keyboard`, `parity/K2-indicator`, `parity/K2-announce`, `parity/K2-handle` (browser); `parity/K2-drag`, `parity/K2-toolbox` (demo). [ADR-0009](./adr/0009-creator-drag-and-drop.md) was deferred to this row and is decided here: **hand-rolled, no library.** Constraint 2 had already removed the cheapest option, and what was left of the case for a library was thin — the expensive part of drag-and-drop is the keyboard grammar and the live-region vocabulary, which Phase 1 already paid for in `useReorder`; adopting one now would mean auditing *its* keyboard story against J4 and J5 instead. A library that owns a portal and a drag layer is also a second opinion about the Creator's markup, underneath a decision we had just made to leave that to the host (ADR-0021, ADR-0022). **The model is a list of insertion slots, not element indices.** A slot is a container and an index, so *n* elements offer *n + 1* of them — which makes a toolbox drop and a reorder the same operation (differing only in whether the thing exists yet), makes the drop indicator a model value rather than a pixel, and makes the keyboard story a walk rather than a second gesture to learn. **Structural edits are definition-in, definition-out, re-parsed**, while property edits stay in the model. Adding an element correctly means a name nothing has taken, a value host, layout and a place in the logic graph — which is what `parseSurvey` is, and re-implementing it incrementally is how the Creator becomes the only place a survey can reach a state the parser would never produce. Three things follow, all wanted: K6 inherits a stack of definitions rather than a command per operation; every drop exercises [ADR-0002](./adr/0002-round-trip-fixed-point.md)'s fixed point; and element identity does not survive an edit, so the selection is re-resolved by name. **A Creator drag previews and commits once** — the opposite of C9's ranking, which applies every move live. Applying per pointer-move would re-parse the survey and rebuild the canvas under the designer's pointer, destroying focus on every keystroke of a keyboard drag; and an indicator is a preview by another name, so a model that applies live has nothing to indicate. Escape abandons rather than reverses. **Constraint 3's letter is not met and its purpose is:** `useReorder` is not the vehicle — its contract is one index space where this has two, and it applies live where this previews — but the grammar is identical down to the sentences, with `reorderAnnouncement` exported and reused, so a designer who has used a ranking question already knows this. **Dragging is on a handle, not the element**, because the arrow keys already mean something inside a radio group, a rating and a ranking list, which is most of §C. A press does not begin a drag: the first *move* does, or focusing a handle would announce a placement grabbed and instantly abandoned. The toolbox item's click is owned by the placement rather than the toolbox, because a drag ends in a click and a toolbox that appended on its own put two questions on the page for one gesture. Slot geometry is nearest-centre then which-side-of-it, along whichever axis the pointer is further out on, so it reads as above/below in a column and left/right in a `colCount: 2` row — proven in the demo, which is two columns. Eleven mutants die, including the reorder off-by-one, which first **survived** a test that moved to the end of the list where an index past the end and one before it slice identically. **Dropping into a panel closed the row.** `parity/K2-nesting`, `parity/K2-nesting-refusals`, `parity/K2-slot-walk` (unit); `parity/K2-nesting` (browser and demo). The blocker was never the placement model — it was that a panel's children are drawn by the respondent's own panel renderer, in markup the Creator does not own. The answer was a **decorator on `PageElementSlot`**, which is already the one wrapper every page element passes through in every container ([ADR-0019](./adr/0019-deep-runtime-modules-and-rendering-seam.md)). A question inside a panel is now adorned by exactly the same code as one on a page, and `PanelRenderer` needed no change at all. The alternatives were worse: a design-mode panel renderer would be two panel layouts to keep in step, and a `designMode` flag threaded through the registry would make twenty renderers know the Creator exists. **A slot names its list, so this was mostly already built** — the same `applyPlacement`, the same off-by-one, the same keyboard grammar. What genuinely changed: a move now spans *two* lists, so `canPlace` stopped looking for the element in the list it was moving *to* (which had silently refused every cross-container drop), and the pair of slots meaning "where it already is" now applies only within one list. A container cannot be dropped into itself or its own descendant — the classic tree bug, and the one that produces a definition no parser can read rather than merely a page that looks wrong. The arrow keys walk a **flattened list of every slot on the page in reading order**, so going into a panel needs no second gesture. **What the definition could not answer.** An empty panel serializes without its `elements` at all — canonical form elides an empty collection (ADR-0002) — so by shape it is a text question, and guessing containers from the shape made a panel a designer had just added the one place on the canvas nothing could go. The registry knows, so the caller that has one now says; the geometry marks an empty container so it can be aimed at, since it has no child to aim at. **A defect this surfaced:** `PanelRenderer` drew `visibleElements`, so a question hidden by `visibleIf` inside a panel became the only element in a survey a designer could not reach — the page-level list never filtered, so the inconsistency had been waiting for somebody to nest something. Eleven mutants die. A twelfth is **equivalent and left alone**: which of two coincident element centres wins a distance tie is unobservable, because a panel wrapper carries its own adorner and never sits exactly on top of its child |
| K3 | Live design surface rendering real questions (WYSIWYG via @kajay/react), selection, inline title editing | ☑ | `parity/K3-design-surface`, `parity/K3-design-mode` (unit); `parity/K3-design-surface`, `parity/K3-selection`, `parity/K3-inline-title` (browser); the same three plus an axe sweep (demo). The questions are **the real ones**, drawn element by element through the registry [ADR-0019](./adr/0019-deep-runtime-modules-and-rendering-seam.md) already made public — so a question type the Creator has never heard of renders correctly and nothing here knows what a radio group is. Each sits in `PageElementSlot`, the renderer's own layout wrapper, now exported for this: `colCount`, `width` and `startWithNewLine` mean on the canvas exactly what they will mean to a respondent, and re-writing that wrapper here would put I5's decisions in a second place to drift from the first. **The survey is put in design mode**, a third contributor to `isReadOnly` beside the authored property and E4's preview — runtime state, never serialized, because `setReadOnly` writes into the definition and a Creator that reached for it would stamp every survey it opened with a flag the author never wrote. That is what stops a click answering, through E7's existing refusal rather than anything new. Selection has two paths deliberately: a click anywhere selects, and the adorner carries a real `<button>` because a click is not a keyboard. Clicking the background clears it, guarded on the event target — without that guard the capture-phase select was immediately undone by the same click bubbling up, and nothing could be selected at all. Every edit goes through one `change` chokepoint, which is what K6's undo stack will wrap. **Editing a localized title edits it in place**: a title authored as `{ default, fr }` keeps every other language, because overwriting it with a plain string would drop them the moment somebody fixed a typo and nothing about typing in a box suggests that. **Departure from "inline":** the title editor sits in the adorner and the rendered question keeps its own title, so a designer sees both — the field they are editing and the label a respondent will read, updating live. An editor drawn *over* the rendered title would have to guess at its position, and one that suppressed it would make the layout on screen a lie |
| K4 | Page management: add/remove/reorder pages, page adorners | ☑ | `parity/K4-add-page`, `parity/K4-remove-page`, `parity/K4-landing`, `parity/K4-page-surface`, `parity/K4-reorder-pages` (unit); `parity/K4-navigate`, `parity/K4-add-page`, `parity/K4-remove-page`, `parity/K4-reorder-pages`, `parity/K4-page-adorner` (browser); the same plus an axe sweep (demo). **Reordering pages needed no second drag implementation.** K2's `DropSlot` gained a `list` — the survey's pages, or a page's elements — and everything else was already right: the same `applyPlacement`, the same off-by-one moving downwards, the same pair of slots that mean "where it already is", the same keyboard grammar and the same sentences in the live region. That off-by-one had already **survived a mutant once**, which is the whole argument against a second copy of it. One placement now drives two lists, so each indicator checks that a slot is aimed at *its* list — and the first two tests of that could not tell the difference, because the slot indices in them could never collide. Both mutants survived until the tests were rewritten to aim a page drag at a slot number the canvas also has an element at. **Add, remove and go-to are not placements**, and are modelled separately: nobody drags a page into existence, and nothing in the toolbox is a page. Adding appends and *opens* the new page, because a designer adds one in order to put something on it. **Removing takes the questions with it** — that is the operation, not a side effect, and K6's undo is what makes it safe to mean. **The last page can go**: refusing would mean a designer cannot delete a page they do not want without first adding a replacement, and a survey with no pages is the state every new one starts in, which the canvas already renders and says so. The canvas lands on the page that took its place, or the one before it when the last went — but only when the page removed was the one being looked at, which a surviving assertion caught. Switching page clears the selection, because leaving it would have the property grid editing something off screen. Page names come from the **survey-wide** pool, since `{page1}` in an expression has to mean one thing. **Page adorners, in both senses:** a `PageNavigatorPanel` piece (ADR-0021) listing every page with `aria-current` on the open one — not colour alone — plus move and delete; and a header on the canvas itself, which is where a page finally becomes *selectable*. K3 could select every element except the one containing them. Its title editor sits beside the heading rather than over it, K3's decision applied to the element K3 could not reach; an untitled page is listed by its name, greyed, because a blank row would be unreachable by voice or by screen reader even though a page without a title legitimately renders without one (E1). **A found defect, fixed here:** the drop indicator drew on slots the model would refuse — the position an item already occupies — promising a move it was about to decline. The keyboard never reaches that state because the arrows step over it, so only a pointer could find it, and only an E2E scenario could prove it. Eleven mutants die |
| K5 | Copy/paste/duplicate questions and panels; convert question type in place | ☑ | `parity/K5-freshen`, `parity/K5-duplicate`, `parity/K5-clipboard`, `parity/K5-convert` (unit); `parity/K5-actions`, `parity/K5-duplicate`, `parity/K5-paste`, `parity/K5-convert` (browser); the same plus an axe sweep (demo). **Every one of these went through `applyEdit` and is undoable without a line about undo** — which is exactly what K6 was built to buy, and this row is the first test of that claim. **Copying a thing is not the hard part; making the copy fit to live beside the original is.** Names must not collide, or the copy and the original both answer to one name and `getQuestionByName` returns whichever the parser saw first. And **references inside the copy must follow the copy**: a duplicated panel whose second question says `visibleIf: "{first} = 'yes'"` means *its own* first question. Renaming without rewriting leaves a copy that looks right and is quietly wired to somebody else's questions — worse than one that fails loudly, because nobody notices until a respondent does. References *out* of the fragment are deliberately left alone: a question copied away from a `consent` that was not copied still means that `consent`. The rewrite covers **every string, not only the properties the registry calls expressions** — `{who}` in a heading or in HTML is the same reference by the same syntax (B6's piping), and covering `visibleIf` but not the title above it would leave a copy that behaved correctly and read wrongly. It is boundary-aware, so `{who}` does not match inside `{whoever}` while `{who.city}` and `{who[0]}` both do. **A copy counts on from the original**: `who2`, not `who1` — the second reads as an unrelated question that happens to sort next to it. Deliberately not `uniqueName`, which numbers from one because a *new* `text` from the toolbox is the first of its kind. **The clipboard holds a definition fragment, not an element**, so it survives any number of re-parses, a page change, or never being used; it is kept in memory rather than written to the system clipboard, because that is a `navigator` call a core package may not make — the architecture rule pointing at the right boundary rather than getting in the way. A host wanting copy between tabs reads `clipboard` and writes it wherever they like. Paste lands after the selection, and at the end when nothing is selected: "somewhere" is not a useful answer. **Conversion keeps what the target type declares and drops the rest** — a text question has no choices, and carrying them invisibly would mean they reappear on converting back, which is state nobody can see or edit. K6's undo is what makes that safe to mean. **A panel is not convertible in either direction**: converting one would drop the elements the target has no notion of, which is a delete dressed as a change of type, and the mirror case would put an empty container where a question used to be. **Two defects the tests found.** Conversion consulted `getProperties` alone and threw away `choices` — a *child collection*, not a property — on radiogroup→dropdown, the single most obvious conversion anybody will ever do. And `copy` changed what the screen shows (whether Paste is available) while announcing nothing, so the button stayed disabled with something on the clipboard; it now announces without recording, which is the distinction this model rests on. **A third design-system primitive**, `Select`, earned its place the way [ADR-0022](./adr/0022-design-system-primitives.md) asks: a type picker cannot be drawn out of a button and a text field. `{ value, label }` rather than bare strings, because a select is exactly where the two come apart. Thirteen mutants die. **Deleting an element** is not one of this row's four operations and was called out here as missing; it now has its own row, K7 |
| K6 | Undo/redo across all designer operations | ☑ | `parity/K6-undo`, `parity/K6-undo-place`, `parity/K6-coalesce`, `parity/K6-depth` (unit); `parity/K6-buttons`, `parity/K6-shortcut` (browser); `parity/K6-undo`, `parity/K6-shortcut` (demo). **A stack of definitions, not a stack of commands** — the return on [ADR-0009](./adr/0009-creator-drag-and-drop.md) decision 3, which is why that decision was worth making. Every structural edit already produces a definition and re-parses, so undo is "parse the previous one" rather than an inverse operation per command. A command stack needs every future editing feature to write its own inverse, and the one that forgets is not discovered until somebody presses undo — deleting a page, which takes its questions with it, would be the obvious candidate. Here it costs nothing extra. The trade is honest and bounded: an entry is a whole survey definition, so this buys never being wrong with memory, capped at 100 edits. **Two chokepoints, both recorded**: `applyEdit` for structural edits and `change` for the ones that mutate the model in place. Between them nothing reaches the survey unrecorded, and `applyEdit` is public because K5 and a host will both have edits `DesignSurface` does not name. **An entry carries where the designer was**, not only the survey: restoring the definition alone would be correct and disorienting, changing a page nobody could see. **A refused edit records nothing** — an entry that undoes nothing is worse than no entry, because pressing undo then appears to do nothing at all. **Typing coalesces**: every keystroke of a rename arrives as its own `setTitle`, and giving the letters back one at a time is not what anybody means by undoing a rename. Coalescing is keyed on what is being edited rather than timed — a timer would make how much an undo gives back depend on how fast somebody types, and would need a clock in the Creator to be testable at all. A run ends when the selection moves, the page changes, or an undo jumps through time. **`Ctrl+Z` is bound to the canvas, never to the document.** A library that grabbed it globally would take it from the rest of a host's application. The public `DesignSurfacePanel` owns the canvas-scoped binding, so a custom host gets the safe behavior by composing the piece rather than importing a loose shortcut parser. Both spellings of redo are accepted, because `Ctrl+Y` and `Ctrl+Shift+Z` are each somebody's muscle memory. **Inside a text field the key belongs to the field**: somebody mid-rename means "take back that letter", and the letters they typed are on no stack of ours. Buttons as well as keys, and not only for discoverability — a disabled button says there is nothing to undo, where `Ctrl+Z` on an empty stack looks exactly like `Ctrl+Z` on a broken one. **A checker was corrected rather than worked around.** The DOM-free architecture rule matched `this.#document`, a private field it had no business reading as the DOM global; its own comment said to fix it properly if it bit a second time, and it had. The carve-out is now asserted against known samples on every run, because a matcher that quietly stopped matching would report a clean gate. Thirteen mutants die, including that one. **Not in scope:** K5's copy, paste and type conversion have nothing to register — they will go through `applyEdit` and be undoable by construction, which is the point |
| K7 | Delete a question or panel | ☑ | `parity/K7-delete` (unit, browser and demo). **A row added rather than a feature slipped in.** No §K row covered deleting an element — K2, K3 and K4 each had a reason not to own it, and K5's words are copy, paste, duplicate and convert — so the gap was named at the end of K5 and is closed here under its own heading, the way B11 was. A Creator that could delete a page but not a question was not something to leave implied. **It cost almost nothing, which is the point.** `removeElementFrom` is a definition in and a definition out through `applyEdit`, so it is undoable without a line about undo, and deleting a panel brings its whole subtree back on the first press — nothing had to write an inverse for it. That is K6's bargain paying out a second time. **What is selected afterwards is the neighbour**, or the one before it when the last went. Clearing the selection would be defensible and worse: a designer deleting the third of five questions is still working in the middle of a page, and an empty property grid reads as having lost their place rather than removed one thing. **`Delete` removes the selection; `Backspace` deliberately does not.** Backspace is the "go back" reflex and the easiest key on the board to hit by accident. Undo makes either recoverable, but only one of them makes a designer wonder what just happened. Both stand aside when focus is in a text field — somebody mid-rename means "that character", not "the question I am in the middle of naming" — which is the same rule K6 set for `Ctrl+Z`, now shared by one handler rather than two. Six mutants die, one of which had to be rewritten: the first was *equivalent*, guarding on a condition the code above had already ruled out, so it could not have failed |

## §L — Creator: property grid

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| L1 | Property grid generated from the metadata registry: categories, ordering, editor type per property type | ☑ | `parity/L1-grid`, `parity/L1-categories`, `parity/L1-editors`, `parity/L1-labels`, `parity/L1-set`, `parity/L1-rename` (unit); `parity/L1-grid`, `parity/L1-editors`, `parity/L1-rename` (browser); the same plus an axe sweep (demo). **Nothing in the grid names a property or a type**, which is the row's claim and the same one K1 made about the toolbox: the rows are `registry.getProperties(type)`, the editor comes from the declared property type, the label is derived from the name and the hint is the registry's own `description`. A custom question type brings its own properties, and `addProperty` on a built-in one is editable with nothing added here — **which closes A4 and A5**, both of which had been partial on this row alone. **Sections are in the Creator, not on the descriptor**, for K1's reason — a survey answered on a server has no property grid — and the table is short in a specific way: it names only the properties that are *not* General. That is the opposite of the toolbox's fallback and the right way round here, because an unlisted toolbox item is an unknown type among known ones, while an unlisted property is usually the one the designer opened the grid to find; the alternative is a table that has to name every property of every type to stay useful. **The Logic section is derived, not listed** — `isExpression` is already on the descriptor because more than one thing needs to know it, so a host's own expression property lands there with no entry, and "every row in Logic holds an expression" becomes a claim §M's logic editor can rely on. The cost is stated: `requiredIf` sits one section from `isRequired`. **Ordering within a section is the registry's**, which is inheritance then declaration order and therefore *serialization* order — a designer who has read the JSON finds the properties where they left them, and there is no second ordering to keep in step. Labels are derived rather than tabled for the same reason the categories are short: a table of human names would be missing an entry the day a property arrived, and would leave a host's custom property as the one row in the grid with a machine name. Acronyms survive (`showTOC` → "Show TOC"); where the derived label reads oddly ("Col count") the description sits under it, wired with `aria-describedby` — a hint only sighted users get is the same hint missing. **What is typed and what is stored are two different strings**, so a field holds a draft: a number field is empty for a moment while somebody retypes it, `1.` is on the way to `1.5`, and JSON is unparseable for as long as it takes to type an object. A field driven straight from the model would write `0` the instant a number was cleared and put it straight back on screen. The draft is re-seeded when the value changes *underneath* it — an undo, a different selection, the adorner's own title editor — and not when it changes because of it. **`name` is the exception and commits on blur**, because renaming rewrites every reference to it: per keystroke would take `{who}` through `{w}`, `{wh}`, `{who}` on the way to `{whom}`, each a rename of a name that never existed, each its own undo entry, each re-parsing the survey underneath the field being typed in. The rewrite is K5's own with a map of one — a rename that changed only the `name` key leaves a survey that still parses, still renders and quietly stops working. Blank names and collisions are refused, and the field puts the old name back rather than showing one the survey does not have. **A localized value is edited in place**, K3's title decision generalized to every localizable property, which is what stops `description` and `placeholder` behaving differently from `title` — and only where the registry says the property is localizable, so a `json` property that happens to hold `{ default: … }` is data rather than a translation. A `Checkbox` is the fourth design-system primitive and earns its place on [ADR-0022](./adr/0022-design-system-primitives.md)'s terms exactly as `Select` did: a generated grid is largely booleans and there is no drawing one out of a button, a field and a picker. **A page has a grid with no code about pages** — K4 made it selectable and it is a registered class like any other. Twenty-five mutants die. **Named gaps:** the registry declares a *type*, not a domain, so `titleLocation` — a string whose description reads "default, top, left or hidden" — is a text field rather than a picker; inferring a domain by parsing English prose is not something to build, and a declared domain is §L4's. The `literal` editor infers from the text (`true` is a boolean, `42` a number), so the *string* `"true"` cannot be typed into one. And expression properties are edited in place, so the logic graph is rebuilt on the next re-parse rather than on the keystroke — invisible on a design-mode canvas, and what §M3's preview will do anyway |
| L2 | Specialized editors: choices editor (with fast entry), validators editor, expression editor with autocomplete, localizable-string editor | ☑ | `parity/L2-collections`, `parity/L2-collection-edits`, `parity/L2-fast-entry`, `parity/L2-fast-entry-locale`, `parity/L2-fast-entry-edit`, `parity/L2-suggestions`, `parity/L2-token`, `parity/L2-translations` (unit); `parity/L2-collections`, `parity/L2-fast-entry`, `parity/L2-expression`, `parity/L2-translations` (browser); the same plus the L1 axe sweep (demo). **The choices editor and the validators editor are one editor**, because they are one thing: child collections the registry declares. So are matrix columns, matrix rows and multiple-text items, and all of them are edited with no code that names any of them. What *looks* specialized is read off the registry — a collection with a **shorthand** gets fast entry, because a shorthand is exactly the claim that a child has a scalar form a line of text can carry; a collection whose base type has several concrete subclasses gets a type picker, which is why validators have one and choices do not. Each child is edited by **L1's own fields**, so a validator's `minValue` is a number field for the same reason a rating's `rateMax` is; the ids carry a scope, or a question's `visibleIf` and the `visibleIf` of the third choice inside it would be one field with three labels. **`elements` is deliberately absent** — what is on a page is the canvas's, and a second way to reorder it here would be a second place for the two to disagree. Every collection edit is **structural, through `applyEdit`**, which matters more than it looked: a validator is a rule the parser registers with the logic engine, and one pushed into the model's array would serialize correctly and never run. **Fast entry is a bulk rewrite that keeps what a line cannot say.** An item whose value is unchanged keeps its `visibleIf`, its `imageLink` and anything a host added — matched on value, not on position — because rebuilding from the text alone would mean fixing a typo on one line silently deleted the conditional visibility on another, a fast entry only safe on lists nobody has customized. A localized `text` is edited in place, L1's rule one level down. It commits **on blur**, so a rewritten list is one edit and one undo. **The expression editor is a combobox, not a `<datalist>`**: an expression is `{who} = 'yes' and {age} > 18`, and a datalist matches its options against the field's *entire* value, so it would stop offering anything after the first character. Completion is over a **token**, which is a model concept in `creator-core` and testable without a DOM: `{` opens a reference and a bare word is a call, so `{iif(}` — an expression the parser rejects — is never offered. Matching is on what a name *starts with*, since a substring match on forty questions offers most of them. The names come from the survey being designed, including a host's own registered function, which needed `Survey.functionNames` — declared for the same reason `isExpression` is, that a list kept elsewhere is wrong the day somebody uses the extension seam. ARIA 1.2 throughout and **focus never moves**: `aria-activedescendant` says which option is current, the arrows walk and wrap, Enter and Tab accept, Escape closes without losing what was typed. A question is not offered its own name, because a `visibleIf` referring to itself is a cycle. **The localizable-string editor sits beside the row's field, not instead of it** — the field above edits whichever language the survey is being read in, which is what a designer wants almost always. The languages offered are the ones already written, plus `default`, plus the survey's own; adding one adds a **field**, not an empty translation, because `{ fr: "" }` in a definition is something an author wrote. Clearing the last translation gives the plain string back, since `{ default: 'Name' }` and `'Name'` mean the same thing and leaving the object would make a survey that had once been translated permanently different from one that never was. **A `Textarea` is the fifth design-system primitive** (fast entry needs one, and §M's JSON and theme tabs are the next two callers); the combobox attributes are props on `Input` rather than a `Combobox` primitive, because the behaviour is ours and all a host's component is asked to do is forward six attributes. Forty-nine mutants die across the two halves. **Two survivors were dead code, both removed**: a fallback offering a collection's abstract base type when nothing concrete descended from it — which would have produced a child the registry refuses to create — and a `default` seeded into the locale set that the return already prepended unconditionally. A third is **equivalent and left alone**: whether a child is labelled by its `name` before its `value`, since no registered child type declares both. **Named gaps:** completion is on the token at the **end** of the field, so editing the middle of an existing expression offers nothing — the alternative needs a primitive that exposes its `selectionStart`, which is a much larger thing to ask of a host's design system than six attributes. `templateElements` on a repeating panel is a collection of page elements, so it is excluded here and the canvas does not draw it either: **a `paneldynamic`'s template is still not editable**, which is a real gap and belongs to a §G-facing row rather than to this one |
| L3 | Property visibility/dependency rules (visibleIf on properties), read-only properties | ☑ | `parity/L3-visibility`, `parity/L3-read-only`, `parity/L3-conditions` (unit); `parity/L3-visibility`, `parity/L3-read-only` (browser); the same (demo). **The condition is declared on the `PropertyDescriptor`, in the survey's own expression language.** `otherText: visibleIf "{showOtherItem} = true"` is parsed and evaluated by the same tokenizer, parser and evaluator a question's own `visibleIf` goes through, so a host writing one is not learning a second language and the operators, functions and truthiness cannot drift from the runtime's. It sits on the descriptor rather than in a Creator table for the reason `description` and `isExpression` do: **the registry is where a property says what it is**, a dependency table kept in the Creator would be missing an entry the day a property arrived, and a host's own `addProperty` would have no way to say the same thing — which a test proves by declaring one. That is a departure from L1's placement of *categories*, and deliberately: which drawer a property is filed under is a decision about a designer, while "`currency` means nothing unless `displayStyle` is `currency`" is a fact about the format that the runtime already implements. **Nothing in any runtime reads either condition** — a value whose condition is false is still stored, still serialized and still round-trips (ADR-0002 rule 3), because hiding a field is not deleting what somebody wrote. The scope is the element's **own resolved properties**, not the survey's answers: a property grid edits a definition and there are no answers on a design surface at all, and resolved rather than authored so a condition reads what the designer sees in the field beside it. **Visibility and read-only are different things, not one thing with a flag.** An invisible property means nothing in this element's current shape, and a field that does nothing is worse than no field because a designer fills it in; a read-only one means something they must not change *right now* — `isRequired` under a `requiredIf` is exactly that, since the value matters again the moment the expression is cleared, and hiding it would silently lose a setting. **The condition has three answers, not two.** No condition, one that will not parse and one that fails to evaluate are all "undecidable", and the safe direction differs by caller — an undecidable `visibleIf` shows the property and an undecidable `readOnlyIf` leaves it editable — so a bare boolean could not carry it, and folding them together is how a typo in somebody's registration makes a property unreachable with nothing on screen to say why. Read-only follows **E7's rule rather than inventing a second one**: `readonly` on a text field and `aria-disabled` on a checkbox, never the HTML `disabled` attribute, so the control keeps its place in the tab order and stays readable. `readOnlyAction` is the public adapter vocabulary; handler refusal stays inside the renderer or headless model that owns the edit, rather than exporting the renderer-private `whenEditable` helper. The refusal is on the **handler**, not the click, which is E7's own finding: React synthesizes `onChange` for a checkbox from the click itself, so cancelling the default stops the browser toggling the box and not the model being written. Conditions are parsed once per surface through an owned `ExpressionCache`, since the grid is rebuilt on every keystroke of a property edit. **Thirteen built-in properties now declare a condition** — `otherText`, `noneText`, `choicesFromQuestionMode`, `setValueExpression`, `requiredErrorText`, `step`, `allowResize`, `currency`, `maximumFractionDigits`, `progressBarType`, `showTimerPanelMode`, `addRowText`/`removeRowText`/`confirmDeleteText` — and one declares `readOnlyIf`. Adding them **changed three L1 tests**, which is the feature being visible where it should be: a text question's `step` and an unrequired question's `requiredErrorText` stopped being in the grid, and the exact-list assertions had to be told. The contract carries both fields, because a second runtime's authoring tool cannot infer them and re-deriving would mean a dependency table per language. Fourteen mutants die. **Named gap:** a condition can only read *properties*, so "`rateMin` is ignored once `rateValues` is authored" — a dependency on a child *collection* — cannot be declared, and is not |
| L4 | Custom properties appear automatically; property grid customization API (hide/reorder/custom editors) | ☑ | `parity/L4-customization` (unit); `parity/L4-customization`, `parity/L4-editors` (browser); the same (demo). **Half of this row was already true and tested at §L1**, and is cited rather than rebuilt: a custom property appears with nothing added to the Creator, one added *after* the survey was parsed still shows its registered default, and §L3 proved a host's own property can declare a visibility condition too. What this row adds is the other half — that a custom property is not a second-class citizen of the grid it appears in, and that a host can disagree with the generated one. **The generated grid is complete, which is what makes it right by default and occasionally wrong for one product.** `PropertyGridOptions` is where a product disagrees: `hidden`, `titles`, `categories`, `categoryOrder`, `order`. Every field is a **refinement of what the registry already said, never a replacement**, so a property it does not mention keeps its declared section, its derived label and its place — a host who wants one thing different says one thing, and a test asserts that everything unmentioned is byte-for-byte what it was. **A plain value, not a class**, unlike K1's stateful toolbox: nothing here is designer state — the search term was, which is why *that* is stateful — so there is nothing to announce and nothing to keep in step. It is deliberately JSON-shaped, because §N2's "configure the Creator" is the same decision arriving from a host's configuration file. **It is a prop on the panel, not something the surface holds**, which is [ADR-0021](./adr/0021-creator-composition.md) working as intended: a host showing an author's grid in one place and a reviewer's in another wants two, and neither is state the survey has an opinion about. **Hiding needs no separate mechanism for sections** — an empty section is already not drawn (L1), so hiding every member of one removes the heading with it; and the same list hides a *collection*, because to a host "do not show the validators editor" and "do not show `valueName`" are one kind of decision. `order` is **one list, not a list per section**: a host reordering a grid is thinking "put `isRequired` near the top", not "position 3 of Validation", so a named property goes first *within whichever section it is in* and everything unnamed keeps the registry's order behind it — which is serialization order, the thing L1 chose on purpose. A host's `categoryOrder` replaces ours outright rather than merging, because a merge of two orderings is a third nobody wrote; sections they did not name still follow, so naming one does not hide the rest. **Custom editors are a function, not a map**, and the collision is real: a map keyed by name cannot also be keyed by editor kind without `text` meaning either "the property called text" — which `itemvalue` and every validator have — or "every text field". A resolver lets a host say `row.name === 'helpUrl'` or `row.editor === 'json'` or both, with nothing to disambiguate; it is the shape `IsContainerType` took for the same reason. It is deliberately **not** part of `CreatorComponents`: that map is ADR-0022's design-system seam ("draw every button with mine") and this is a different question ("draw *this property* with mine"). The editor is handed the row, the element, the surface and **the id the row's `<label>` already points at**, rather than left to invent one — a replacement that invented its own would be a field with no label attached to it. **The demo closes L1's own named gap through the seam rather than in the library**: `titleLocation` is a string whose description reads "default, top, left or hidden", L1 declined to guess a domain by parsing English prose, and the host supplies a `<select>`. That is the argument for the seam made concretely. **Two defects this found.** `CreatorSelectProps` had no `id`, so a replaced editor built on the select primitive could not attach its label — found the first time the demo's picker was written. And the grid's section `data-testid` was unscoped, so a select question with choices drew two `properties-General` elements; scoped now, like the field ids L2 had already had to scope for the same reason. Fifteen mutants die. **Named gap:** the *collection* editor itself is not replaceable — a host may hide `choices` or retitle it, but the list-with-disclosures is the Creator's. That is a bigger seam than a field and belongs to whoever needs it |
| L5 | Survey-level and page-level settings surfaces | ☑ | `parity/L5-survey-settings`, `parity/L5-page-settings` (unit); the same (browser); the same (demo). **Page-level settings needed no code at all** — K4 made a page selectable and L1 generated its grid, so selecting a page in the navigator has shown its `title`, `colCount`, `visibleIf` and `maxTimeToFinish` since L1 landed. This row is cited for it rather than claiming it. What was genuinely missing was the survey: **it was the one thing in a survey nobody could select**, and every §L row until now said so. **The survey is a registered class, so making it selectable was the whole of it.** `survey` declares twenty-four properties, `Survey` is a `SurveyElement`, and `surface.properties(survey)` therefore works with no property-grid code about surveys anywhere — the same bargain K4's selectable page collected. **The survey has no name, and that is not a gap**: it is the thing names are *unique within*. An element or page is selected by name because nothing survives a re-parse by identity and a name is what an edit can hand back; the survey cannot be selected that way at all, and inventing a reserved token would put one into a channel that otherwise holds real names and would collide with a question called it the day somebody used it. So the survey is a **flag**, which is not a compromise: the survey is always there, so "the survey is selected" needs no resolving after a re-parse and cannot go stale. That made a `DesignSelection` worth extracting — two kinds of selection is a concept, one field was not. **An edit that names nothing leaves the selection alone; one that names something means it**, which is what lets the survey survive `addPage` and give way to `duplicate`. Undo carries it separately for the same reason `selected` cannot: without that, undoing while looking at the survey would leave the grid there while restoring a question nobody could see change. **What the survey contains arrives free.** Its `calculatedValues`, `triggers` and `completedHtmlOnCondition` are child collections, so L2's collection editor draws them with no code about any of them — a survey's calculated values became editable as a side effect of selecting the survey. `pages` is excluded, and the exclusion generalised: L2 had kept `elements` out because the canvas owns it, and the rule is now **the base type the canvas and the navigator own**, so a host's own container is excluded for the same reason rather than by name. **The settings surface is a "Survey" entry in the page navigator**, not a piece of its own: that panel is where the survey's *shape* is drawn, and the survey is the root of it — a designer looking for "the whole survey" looks at the list of what it contains. It **selects rather than navigates**, so it carries no `aria-current`: the canvas still shows a page, and claiming "current" for something nobody navigated to is the one thing that list must not get wrong. **A defect this surfaced:** a collection edit resolves its owner by walking the definition for a name, and the survey has none — so `addChild(survey, …)` silently refused. An empty owner now means the root, written once as a named function rather than as a branch in each caller. Fourteen mutants die. **Named gap:** the survey's grid is reached by selecting it, so a host who hides the page navigator has hidden the survey settings with it; `PropertyGridPanel` and `selectSurvey()` are both public, so an assembly that wants a Settings tab has everything it needs, but the library ships no second entry point |

## §M — Creator: editors

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| M1 | Visual logic editor: list rules, build conditions via dropdown UI, actions (show/hide, enable, require, skip, setValue, complete), round-trips to expressions | ☑ | `parity/M1-condition-read`, `parity/M1-condition-refusals`, `parity/M1-condition-write`, `parity/M1-logic-rules`, `parity/M1-logic-edits`, `parity/M1-logic-choices` (unit); `parity/M1-logic-rules`, `parity/M1-condition-build`, `parity/M1-condition-refusals`, `parity/M1-logic-edits` (browser); the same plus an axe sweep (demo). **The builder handles what it can represent and never rewrites what it cannot**, which is the load-bearing decision of the row. A condition is a flat run of comparisons joined by *one* operator, because that is what a table of dropdowns is; `({a} = 1 or {b} = 2) and {c} notempty` is not, so it comes back unrepresentable and stays an editable text field with a note saying why. A builder that "mostly" understood an expression would flatten a designer's parentheses the first time they opened the row, and the survey would change meaning without anybody touching it. A mixed `and`/`or` is refused for the same reason — a table with no parentheses in it cannot say whether `a and b or c` meant `(a and b) or c`. **Printing goes through core's own printer**, never string concatenation, so the round trip is core's problem — already tested — and the spelling is canonical. A test asserts seven expressions come back byte-identical. **A value box holds a value, not an expression**, and the four rules for reading one are exact: braces are a reference, `true`/`false`/`null` are themselves, a *plain* number is a number, everything else is a string. Parsing the box instead would turn a date typed as `2026-01-01` into 2024 — arithmetic, discovered by a respondent rather than by a designer — and `Number()` alone would take `1e5`, `0x10` and `Infinity`, the last of which prints as something the parser cannot read back. **A rule is one action with one condition and exactly one place in the definition.** There is no grouping by shared condition: two conditions spelled differently would not group, and editing a group would quietly edit several properties at once, which is a much larger promise than a logic tab should make. The list is **derived on every read** — M4's decision — so deleting a question takes its rules with it and no second list of logic can drift. `visibleIf`, `enableIf`, `requiredIf`, `setValueIf` and `resetValueIf` are rules; **`defaultValueExpression` deliberately is not**, because it has no condition — it is a value computed always — and a row with an unfillable condition column would be worse than no row. Triggers are rules too, listed alongside, which is where skip and complete come from. **Every edit goes through the surface**, so a condition built from dropdowns is undoable exactly like a drag, and the logic tab, the property grid and the JSON tab are three views of one document that cannot disagree — proven by a demo scenario that builds a condition in one tab and reads it in another. **A new rule is never added empty**: canonical form elides a `visibleIf` of `""`, so a row added with nothing in it would vanish on the next re-parse. The value cell is a **picker when the question has choices and a field when it does not**, read off the model rather than a type name. Twenty-four mutants die, and two of them found **unreachable guards** — a skip that stopped the walk descending into triggers (which produce no rules, because a trigger declares none of the conditional properties) and a clear of the value cell when the operator stopped taking one (which cannot be observed, because the row is re-derived from the definition that already dropped it). Both were removed rather than kept, which is E7's lesson arriving twice in one row. **Named gaps:** a trigger is keyed by its position because it has no name, so the logic tab offers no reordering of them; and an expression edited here mutates the model in place, so the logic graph is rebuilt on the next re-parse — L1's gap, closed by M3's preview. §M is complete |
| M2 | JSON editor tab: two-way sync with designer, syntax + schema error surfacing | ☑ | `parity/M2-json-sync`, `parity/M2-json-errors`, `parity/M2-json-location` (unit); the first two (browser); the same plus an axe sweep (demo). **Two-way, and the two ways are not symmetrical.** Designer to text is free — the definition is already what `serializeSurvey` produces — so the draft is re-seeded whenever the designer changes anything. Text to designer goes through `applyEdit`, which means a hand-edited definition is re-parsed, validated and **undoable exactly like a drag**: [ADR-0009](./adr/0009-creator-drag-and-drop.md) decision 3 paying out where it matters most, because this is the one surface where a designer can replace the whole survey in a keystroke. **A draft is followed only while it is clean** — M3 drew that line for the preview and it is the same line here: type nothing and lose nothing, type something and it is never overwritten because somebody dragged a question on the canvas. **Staleness is the seed against the surface, not the draft against the surface**, which sounds like a detail and is the difference between a conflict notice that appears when both have moved and one that appears on the first keystroke of every edit. A surviving mutant is what asked the question. **Diagnostics do not block applying, and that is the row's real decision.** `parseSurvey` is total: it recovers, keeps what it does not understand ([ADR-0002](./adr/0002-round-trip-fixed-point.md) rule 3) and reports. Refusing a definition the library itself accepts would make this tab a *second opinion* about what a valid survey is — stricter than the file the host loaded at startup, and unable to open a survey the runtime runs happily. So they are surfaced live and applying is blocked only when there is **no definition to apply**: `JSON.parse` refusing the text, or `parseSurvey` refusing the value (a root that is not an object, a `schemaVersion` from a future this build cannot read). Two problem kinds rather than one, because they are answerable differently — the first has a place in the text and the second is about the document. **The location is optional, and that is not defensiveness.** `JSON.parse`'s message is not a contract: one V8 version produces *two* formats, and `Unexpected token ',', "…" is not valid JSON` carries no position at all where `Expected ',' or '}' … at position 8` does. So the offset is read out with the one substring that has survived every rewording, the line and column are counted from the text rather than trusted from the message, and nothing is invented when there is no position — a line number a designer goes to look at and finds nothing wrong with is worse than being told to look at the document. A **diagnostic's location is its JSON Pointer**, because that is what it carries and it is exact; mapping `/pages/0/elements/1` to a line needs a position-aware parse this row does not have, and saying so beats approximating it. Applying **re-seeds from what survived rather than from what was typed**, since applying canonicalises (ADR-0002) — calling the draft clean without that would leave the editor showing something the survey no longer says. A plain `<textarea>`, deliberately: a syntax-highlighting editor is a large dependency with its own opinions about keyboard handling and markup, which is what [ADR-0022](./adr/0022-design-system-primitives.md) says the Creator does not ship — and everything this row promises lives in the *model*, so a host who wants CodeMirror binds it to `text`/`setText` and loses nothing. Eighteen mutants die, one of which found a genuinely **unreachable guard**: `isDirty &&` in front of the staleness comparison could not fail, because a clean draft is re-seeded the instant the designer moves. It was deleted rather than kept, which is E7's lesson about logic no test can reach |
| M3 | Preview tab: run the in-progress survey with device/orientation presets, test data | ☑ | `parity/M3-preview`, `parity/M3-preview-follows`, `parity/M3-preview-data`, `parity/M3-preview-devices` (unit); the same (browser); the same plus an axe sweep (demo). **Its own parse, and that is the whole design.** The canvas holds a survey in design mode where every question reports itself read-only (E7) and a click cannot record an answer; a preview must be the opposite, and must not be able to write into the document being designed either — so it is a *separate* `Survey` parsed from a snapshot of the definition, and answers live and die with the session. That also means a preview exercises `parseSurvey` the way a respondent's browser will, which is the point of previewing at all: **it is what closes L1's named gap**, where a property edit mutates the model in place and does not rebuild the logic graph. An edited `visibleIf` becomes real here and nowhere else. The **host's own seams are passed straight through** — `ParseOptions` for choices-from-URL, host functions and file uploads, and the whole of `SurveyProps` for renderers, theme and sanitizer — because a preview missing them is a designer testing the parts that happen to need nothing. One passthrough rather than a mirrored prop each, so it cannot drift into being a differently configured survey from the one it is previewing. **A restart is automatic while there is nothing to lose.** A designer who tweaks a title should see the title, not a notice about it; one who has filled in three pages should not lose that because somebody fixed a typo — and "nothing answered, no page turned, not finished" is exactly the line between those, so that is the line. Staleness is compared as **definitions**, not by the surface's version counter, which also advances when a designer merely clicks a question: a preview that announced itself stale on every click would be crying wolf. It is memoised on that counter all the same, so the comparison happens once per change rather than once per render. **Test data is the premise of a run, not the first thing answered** — seeded before the run is listened to, or a session with test data could never follow an edit, and re-seeded on every restart because that is what a premise is. Devices are **CSS pixels**, the units `width` and `minWidth` are authored in (I5), so a question that fits at 375 fits on a phone rather than fitting in a picture of one; rotating swaps the two numbers and nothing else, the desktop rotates too (a portrait monitor is a real thing people fill in forms on), and **responsive is the default** because a preview that opened at 375 would make every survey look like it had a mobile problem it does not have. Changing the device never restarts: throwing away a half-filled run to look at it on a phone would make the one question the preset exists to answer the hardest one to ask. **Two counters, not one** — `version` for "redraw" and `run` for "this is a different survey now" — because the panel keys the survey on the latter, and keying on the former tore the field out from under the caret the moment anybody typed. That is asserted on **focus** rather than on the value, which comes back from the model on a remount and cannot tell a redraw from a fresh run. **What the demo found, and why the row's word is "tab":** a preview is a *complete second survey* in the document — the same Next button, the same roles — and rendering it beside the designer made every page-wide query in this repo's own scenarios ambiguous, breaking sixty-nine of them at once. The same lesson K2 learned about a second `role="status"`, arriving several sizes larger. The library is happy either way and the stale notice uses `aria-live` alone for that reason; the demo makes it a tab, and **the session is owned by the assembly rather than the tab**, so switching away to fix a title and back does not cost a designer the run they had going. Sixteen mutants die. **Named gap:** the preview is a live `<Survey>` in the same document, so a survey whose CSS assumes a viewport width sees the *browser's*, not the frame's — a container query or an iframe would be needed for that, and neither is this row's |
| M4 | Translation editor: locale columns, machine-translation seam, import/export strings (CSV/XLSX seam) | ☑ | `parity/M4-translations`, `parity/M4-translation-columns`, `parity/M4-machine-translation`, `parity/M4-translation-sheet`, `parity/M4-csv` (unit); the first three (browser); the same plus an axe sweep (demo). **The table is found by walking the registry, not by knowing what a survey contains.** `isLocalizable` was already on the descriptor for J1's sake and `getChildCollections` already says what holds what, so a choice item's `text`, a validator's message, a matrix column's title and a host's own custom localizable property all turn up here without anything naming them — K1's claim about the toolbox and L1's about the property grid, one level deeper. **It is derived, never stored**: `entries` walks the survey on every read, so a question added on the canvas has a row without being told and a deleted one takes its row with it. There is no second list of strings to fall out of step with the survey, which is the failure mode every translation tool has. **A key is built from names, not positions** — `pages/p1/elements/who/title`, not `pages/0/elements/0/title` — because a translator sends the file back a week later and a question that has moved must not take somebody's German with it to the wrong row. A child with no name is identified by *its own value*, which is what a choice item has instead; position is the last resort rather than the first. **Adding a language opens a column, not a hundred empty translations**: L2's decision about one property, applied to the table. Closing one is refused the moment it holds anything, because there is no such thing as hiding a language — "remove" would have to mean "delete every German string in the survey", which is a real operation and not this one, and making one button mean both is how somebody loses a week's work. **The machine-translation seam is a function and nothing ships.** It is asked for a *batch* — one call carrying every untranslated string — because a service charged per request would otherwise be billed once per title. It fills **only the empty cells**, because the whole reason a person wrote a translation is that the machine got it wrong and running this twice has to be safe. A service that hands back the wrong number of strings is **refused entirely** rather than applied as far as it goes: the alignment is positional, so a short answer does not mean "some failed", it means every string after the gap now carries somebody else's translation. **Rows first, CSV second, and that split is the XLSX seam** — a sheet is a rectangle of strings, CSV is one way to write one down and XLSX is another, and the second needs a binary-format library a core package may not carry. So the public `TranslationSession.toRows()` produces the rectangle, `applyRows()` consumes it, and the package-local CSV codec is one way to write it down. A host hands those rows to whichever spreadsheet library it already has. The **demo owns the file controls**, which is the seam working: a download is an anchor with an object URL and an upload is a file input, neither of which the library may assume about a browser. Columns come back **by their header, not their position**, because a translator who added a language or moved them about still sends back something usable; a sheet with no `key` column is refused outright rather than half-read. Importing reports **what landed and what no longer exists** — a count with no mention of the rest is how a survey ships with a language nobody notices is missing three strings — and an unchanged cell is not an edit, or importing a file nobody touched would be a hundred undo entries. The CSV reader is character-at-a-time rather than a split on commas, because a title with a comma or a line break in it is not unusual and a splitter gets both wrong in a way that looks like it worked. **A real `<table>`**, because a screen reader announces "row 4, column French" from one and nothing at all from a grid of divs, and each cell is named by *both* headers it sits under. Twenty-one mutants die, one of which found a real defect: the readable trail resolved titles in the empty locale rather than the default one, so every row was labelled by its machine name instead of the title a translator would recognise |
| M5 | Theme editor: edit theme JSON visually, live preview, import/export theme | ☑ | `parity/M5-theme-fields`, `parity/M5-theme-paths`, `parity/M5-theme-editing`, `parity/M5-theme-file` (unit); the first three (browser); the same plus an axe sweep (demo). **The editor knows nothing about `@kajay/themes`, and cannot.** `creator-core` may depend on `@kajay/core` and nothing else, so a theme here is the plain JSON object [ADR-0008](./adr/0008-no-surveyjs-theme-import.md) always said it was, and turning one into CSS variables stays the host's call to `themeVariables`. That is the constraint doing its job rather than getting in the way: **the editor works on a host's own theme format for exactly the same reason it works on the shipped one**, and a test edits a format the library has never seen to prove it. What describes the shipped format is a *table* — `builtInToolbox`'s shape a third time — and **a key the table has never heard of still appears**, under a field inferred from the value it holds. **The live preview is composition, not a feature.** The demo calls `themeVariables` on the edited JSON and hands the result to M3's `PreviewPanel`, which is already the real `<Survey>`: what a designer watches change is the survey a respondent gets, no second preview exists to disagree with the first, and this row shipped no rendering code at all. **Clearing a field removes it rather than blanking it**, which is I2's rule rather than a nicety — *what a theme does not name, it does not set* — because a blanked `cornerRadius` reaches the renderer as an empty CSS variable and overrides the stylesheet's own default with nothing. A branch left empty goes with it, or an export would carry `"backdrop": {}` around forever; and a choice offers an explicit `(not set)`, or a choice once made could never be taken back. The **unset state is drawn**, because "the theme does not name this" is a different answer from "empty" and a designer has to be able to tell them apart. **A preset replaces rather than merges** — I3's finding in a different place: a preset merged over the current theme inherits whatever the last one set, which is exactly how switching leaves one wrong colour behind. An imported file that is not a JSON *object* is refused rather than applied, because applying it would blank every variable the survey had. **Not on the survey's undo stack, deliberately**: a theme is a fact about one deployment rather than part of the document, so it has no business in a history of edits to it — `reset` and importing are the ways back. A colour is drawn as a **swatch, not `<input type="color">`**, which accepts `#rrggbb` and nothing else while a theme is free to hold `oklch(…)`, a `var(…)` reference, or nothing at all — a picker would quietly show black for all three. Twenty-one mutants die. **Named gap:** no colour *picker*, for the reason above; a host who wants one and knows their palette is hex can draw the field themselves against `setValue`. §M is complete except M1 |

## §N — Creator: embedding & integration

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| N1 | Embeddable component with controlled JSON (value in / onChange out), save seam (onSurveySaving/auto-save) | ☑ | `parity/N1-save`, `parity/N1-controlled` (unit); `parity/N1-assembly`, `parity/N1-controlled`, `parity/N1-save` (browser); the same plus an axe sweep (demo). **`<SurveyCreator>` is built from nothing but the pieces this package already exports**, which is [ADR-0021](./adr/0021-creator-composition.md) decision 3 and the only thing keeping the pieces real: if the assembly ever needed something they do not expose, that is a missing export rather than a special case. The file is what a host arranging their own layout would have written. **Controlled, with the one honest reading of the word.** A survey being designed is a large stateful model with an undo stack and a selection; a strictly controlled component would be re-created from a prop on every keystroke, and a host who forgot to echo `onChange` back would have an editor that refused to type. So `value` is the document the Creator *opens*, and the two sides are kept in step through one remembered fact — the last definition they agreed on. The Creator's own output coming back is **not** a change; a value that is not our own output is one the host means, so the document is re-opened, **through `applyEdit`** so a designer can take it back rather than having their history reset. Compared as canonical JSON, because every read of `definition` builds a fresh object and identity would either report forever or never. **The save seam has no clock, deliberately.** A debounce would make "did it save" depend on how fast somebody types and every test about it a race. Instead a save in flight *absorbs* the requests arriving during it: only the latest survives, and exactly one more save runs when the current finishes — B2's async-cache shape, keyed on what the work is *for*. A host who wants a delay wraps their own saver, which is the only place that decision can be made well. A saver that throws has failed, which is the same answer as returning false; a stale save **never reports success**, because `saved` between two runs is a state that was true of nothing on screen, and a mutant that moved that line survived until the *sequence* was asserted rather than the final state. Failure is announced in a live region as well as drawn on the button — a save that quietly did not happen is the one thing a designer must never have to guess about. **Two defects the tests found, both about props.** The models were memoised on `registry`, `parse` and the rest, so a host writing `registry={new MetadataRegistry()}` inline — the first thing anybody writes — rebuilt the surface, the undo stack and the selection on every render; a controlled-value test found it by echoing `onChange` straight back. They are now built once, and the save controller likewise calls through a ref so an inline `save={…}` does not lose a save in flight. And an **axe sweep found the tab strip claiming `role="tablist"` without keeping its keyboard contract** — arrow keys, Home and End, a `tabpanel` each tab controls. It is now navigation with `aria-current`, which is what K4's page navigator decided first and for the same reason: a role that promises something it does not keep is worse than no role. **One tab at a time is not a layout preference** — a preview is a complete second survey in the document, which M3 established by breaking sixty-nine scenarios at once; a host who wants them side by side arranges the pieces, which is what ADR-0021 is for. Sixteen mutants die |
| N2 | Configuration: hide tabs, restrict question types, read-only mode, isAutoSave, showJSONEditorTab etc. | ☑ | `parity/N2-types`, `parity/N2-read-only` (unit); `parity/N2-tabs`, `parity/N2-types`, `parity/N2-read-only`, `parity/N2-grid` (browser); `parity/N2-config` (demo). **Configuration is a plain value, not a class**, so it can be written in JSON, stored against a customer and sent from a server: a deployment offering three question types to one tenant and thirty to another has to be able to *say* so somewhere other than in code. The demo's restricted deployment is exactly that — one object literal. **Everything in it is a restriction**, and that is what keeps the shape checkable: no field adds a capability, so a Creator with no configuration is the most capable one and an unrecognised field is harmless. **A restriction is enforced at the edit, never at the button.** A toolbox that drew a shorter list while placement still accepted anything would make it a suggestion — a keyboard drag, paste or host-started placement would walk straight past. The headless `DesignSurface.placement` controller therefore owns the same type and read-only refusal for click, keyboard, and pointer lifecycles, while `convertIn` enforces conversion. A type a designer may not *add* is one they may not *convert into*, or the restriction is a detour rather than a rule; and a **host's own toolbox item is held to the same restriction**, because a curation seam that let a blocked type back in through the side door would be worse than no restriction. **An empty allow-list means none, not unrestricted** — a deployment offering a fixed set of pre-built pages is a real thing, and "empty means unrestricted" is the sort of rule that deletes somebody's restriction by surprise. Toolbox order stays the registry's rather than the allow-list's, so a configuration cannot silently do two things at once. **A read-only Creator is a viewer, not a hidden one**: the canvas, the property grid and the JSON are all still there to read, and selection still works, because a reviewer needs to see the logic to comment on it. Hiding tabs is the separate, composable decision. §L4's grid options travel **inside** the configuration, so a deployment says everything it has turned off in one value rather than in two that can disagree. `isAutoSave` is N1's and cited here. **A configuration is read once**, when the models are built — the same decision, and the same trap, as N1's `registry`: a host changing which deployment they are showing is showing a *different* Creator, and the demo keys the component on it, which is React's own way of saying so. Thirteen mutants die. Free placement predicates remain implementation details of that controller rather than a second public API. |
| N3 | White-labeling: creator theming, localized creator UI strings | ☑ | `parity/N3-strings`, `parity/N3-format` (unit); `parity/N3-strings`, `parity/N3-theme` (browser); the same (demo). **The catalogue is the source of its own key union**, which is J2's pattern for the runtime's words and ADR-0020's for diagnostic codes: adding a label means adding it here, and a key that is not here fails to compile. "English is complete" is therefore a fact about the build rather than a claim in a document — English cannot be missing an entry, because the entry *is* the English. **A separate catalogue from the runtime's**, deliberately: a respondent never sees any of these and a designer never sees "This question requires an answer", so one table would make every host translating a survey read forty strings about drag handles. The two audiences are different people, usually in different languages — which is also why the **Creator's locale is separate from the survey's**: a designer working in German on a French survey wants German chrome and French content, and tying them together makes that impossible to say. **This row is the promise K1 and L1 both made and deferred** — the toolbox's drawers and the property grid's sections said they would stay English until N3, and they do not any more. Both are a **fallback, not a lookup**: a drawer or section the catalogue has never heard of keeps the word a host gave it, or this row and §L4's "name your own section" would contradict each other. A host **registers over** the built-ins rather than replacing them, so renaming one button does not blank the other eighty, and registering twice merges — a plugin adding words to a language the application already registered must not delete them. **White labelling is renaming, not translating**, and the commoner half: the same language, a different vocabulary, which the demo shows by turning Design into Build and Undo into Step back. The Creator's **own chrome is themed apart from the survey it edits** (`creatorTheme` beside `theme`), because an agency's tool is their brand and their client's survey is the client's; the pieces already drew through §I's tokens and [ADR-0022](./adr/0022-design-system-primitives.md)'s primitives, so this row only had to stop the two themes being one. A Creator given no dictionary speaks English and works, which is what keeps the pieces usable alone (ADR-0021). Twelve mutants die, one of which found a **third unreachable guard** in this project's history: a `?? key` last resort that could never fire, because the shipped English is complete by construction and `register` merges. It was removed and the fallback made **total and typed** rather than hoped for. **Named gap:** the built-in toolbox *item* titles ("Single-line input") are not in the catalogue — they are host-curatable data by K1's decision, replaced by re-adding the item, and putting them here would have made the two mechanisms fight |
| N4 | Works under the pack test: creator consumed from tarball in scratch project | ☑ | `pnpm run test:pack` (gate 9), extended to the Creator. **The pack test already packed all five packages — and proved nothing about two of them.** Nothing in the scratch project imported `@kajay/creator-react`, so a broken `exports` map for the Creator's React entry, or a declaration referencing a type it forgot to ship, would have passed every gate and failed on the first consumer. A tarball nobody imports is a tarball nobody has checked. **The smoke scenario now drives the Creator headless**: place a question, edit a property, undo, read the property grid, open the logic, translation and JSON sessions, assert a Creator-built definition is still a round-trip fixed point ([ADR-0002](./adr/0002-round-trip-fixed-point.md), this time through an *installed* package), then a read-only and type-restricted configuration, a white-labelled dictionary and the save seam. Headless is not a compromise — `creator-core` is a core package and may not touch a DOM, so everything the smoke can legitimately reach is already there. **The React half is compiled and deliberately never run.** Rendering needs a DOM this harness does not have, and what a consumer's build actually proves is that the export map resolves and the declarations are usable: `creator.tsx` builds the default assembly configured and white-labelled, then arranges the same Creator **by hand out of the pieces**, which is [ADR-0021](./adr/0021-creator-composition.md)'s claim stated as something a compiler can refuse. It type-checks under every TypeScript in the matrix with `skipLibCheck: false`. Because a checked-but-unrun file reports nothing when it is silently dropped from the build — the worst kind of green — the harness asserts its **emitted output exists**. **The stylesheet ships the Creator's own chrome**, not just the survey's: a consumer who installed the Creator and got an unstyled one would have no way to tell whether that was the library or their own build. Five mutants die, and choosing them was the finding: mutating *source* only ever proved `tsc` works — three such mutants failed to build before the pack test ever ran, because the workspace itself references what they broke. The mutants that mean anything here break the **published surface** — `files` that forgets the stylesheets, a `types` path pointing at nothing, a `dist` narrowed to one file — none of which a workspace symlink can see and none of which the compiler catches |
| N5 | **Overall AC scenario:** build survey covering every §C/§F/§G/§H type in Creator → render → submit → lossless round-trip back into Creator | ☑ | `parity/N5-every-type`, `parity/N5-submit` (unit); `parity/N5-render` (browser); `parity/N5-round-trip` (demo). **The survey is built by driving the Creator, not written out as JSON** — a hand-written definition would prove the parser handles every type and say nothing about whether a designer can produce one. Every element in it is the result of a drop. **The list of types is the toolbox's, and the toolbox is checked against the registry**, so nothing anywhere in this row names a type: a twenty-second one is covered the day it is registered, and covering it *twice by the same omission* — a type registered and never surfaced — is what the registry comparison rules out. The answer table cannot be derived (only a person knows a matrix wants a row *and* a column), so instead its **completeness** is checked: every question type is either answered or listed as computed, and a type nobody wrote an answer for fails rather than being quietly skipped. Submission goes through the ordinary advance gate rather than `complete()`, because what has to be shown is that a page holding one of everything *accepts* the answers. **Two defects, both of the kind only a whole-path walk finds.** First, **every drop produced an element with nothing in it**: a dropdown with no choices, a matrix with no rows, a repeating panel with no template — a question a respondent could look at and could not answer, and three clicks a designer had to make after every single drop. The toolbox now ships starter content, and stops exactly where a question would otherwise be *unanswerable* rather than merely empty: `html`, `image` and `expression` start blank, because their content is entirely the designer's and there is no placeholder for it that is not noise. It lives in the Creator's table rather than as a registered default, because a survey arriving from a server must not grow three choices for having left `choices` out — [ADR-0016](./adr/0016-metadata-owns-property-defaults.md) is why that distinction has somewhere to live. Second, **a matrix or a repeating panel on the canvas threw**: a question that contains questions looks its children up in the renderer registry, `<Survey>` supplies it and the design surface did not — so the one place a designer meets §F and §G in the Creator was a crash. No scenario before this row had put a container question on a canvas. **The loop closes in the demo** rather than only in a test: load one of everything, see all twenty-one adorned, preview, answer, submit, come back to the designer and find the JSON byte-identical — because a response is not an edit, which is what M3 made the preview parse its own survey for. It reuses N1's embed rather than adding a second Creator to the page, which this application has been bitten by twice. Six mutants die, and two of them are aimed at the scenario's own derived guards — a completeness check that cannot fail is decoration. **Closes A4's named gap**: five mechanisms, five proofs, and now one scenario walking all five |

## §P — The reference application (kajay.io)

Placed before §O rather than after it because §O is explicitly *not* acceptance, and
everything above it is. These rows exist because §A–§N proved the library **capable** and
nothing proved it **pleasant** — the host demo is a test harness that consumes the packages
correctly and looks like one. `apps/site` is the first consumer written the way an adopter
writes one: a real framework, a real design system, and no privileged access.

It lives in this repository until the packages publish, at which point it moves to its own
and consumes tarballs — which is the stronger proof and needs a version to install first.

| ID | Feature | Status | Proof |
| --- | --- | --- | --- |
| P1 | The survey renderer works under server-side rendering | ☑ | `parity/P1-server-rendering` (unit). **Found by the reference application, and findable no other way.** A survey placed on a server-rendered route threw `Missing getServerSnapshot, which is required for server-rendered content` from all eight `useSyncExternalStore` hooks in `useSurveyState`. The failure was **silent**: React does not warn, it throws and reverts the *entire page* to client rendering — so a host on Next or TanStack Start got an empty first paint, nothing a crawler could read, and no error anywhere. Ninety-four unit files and forty-four browser files could not have caught it, because every one of them renders in a browser. **The proof is a unit test, and that is not a compromise**: server rendering is defined by the absence of a DOM, so running it in the browser project would test the one environment where the bug cannot occur. `renderToString` builds a string and touches no document — the opposite of the jsdom case the guidelines ban. **The same `getSnapshot` serves both sides** because these snapshots already agree: each reads synchronous state off a model built from the definition, and server and client build that model from the same definition, so there is no client-only value to diverge from. Where that is *not* true the answer is a different function — `useMatrixLayout` already did this correctly, stating that a server with no screen renders the table, which is why it was the one hook that never failed. Five assertions, and all five fail without the fix: the questions, the choices, a host's existing answers (E6's save-and-resume is the common case for a server-rendered survey), the completed page, and an empty survey that must not throw. **Named gap:** the Creator is not server-rendered and is not intended to be — it is a live document with an undo stack and a drag gesture, and the site wraps it client-only on purpose |
| P2 | The renderer draws its chrome through the host's design system | ☑ | `parity/P2-primitives` (browser). **[ADR-0022](./adr/0022-design-system-primitives.md)'s second half, scheduled since Phase 2 and unbuilt until the reference application made it visible.** `creator-react` has drawn through a primitive map since its first commit; the renderer never did, so a shadcn host got a Creator built from their components and a survey built from ours **in the same frame** — the exact "two design systems on one page" outcome the ADR exists to prevent. Slice 0 photographed it: native browser radios directly beneath shadcn buttons. **Five primitives, and every control in the package draws through one.** Button came first — twenty-six of the forty-eight native controls, the most visible thing a design system owns, and every one a plain action — then Input, Textarea, Checkbox and Radio. Each was declared only once something drew through it: a map entry nothing uses is API a host can supply and watch do nothing. **Checkbox and Radio are two entries sharing one props interface**, because the props are identical and the components are not — a design system ships them with different keyboard contracts, and folding them into one entry with a `type` prop would hand every host the branch we had just refused to write. The library picks between them where arity is a design-time fact and, for §C10's image picker, where it is a *property*. **`reselect` is the contract's least obvious clause and its most necessary**: a native radio raises no change event when the already-chosen option is picked again, and that is exactly how §C8 and §C10 let a respondent take an answer back — so the default listens on click when it is set. A substituted primitive that ignores it leaves a respondent unable to clear an answer with nothing on screen to explain why. Converting the boolean switch also removed a dependency on `event.target.checked`: a design system's Checkbox has no event target, so the toggle is now expressed against the model, which already knew. **The defaults render byte-identical markup**, which is what makes the two hundred and ninety browser tests and two hundred and nine end-to-end scenarios this change's regression net rather than work redone beside it — all of them stayed green, and a red one would have been a real behaviour change. Substitution is proven separately, because the defaults would keep every one of those green whether the seam worked or not: a host button draws, keeps the class the renderer asked for, receives the click, and — the contract's sharpest clause — still carries §C9's drag gesture, which arrives as pointer handlers spread onto a ranking row and would break *silently* if a primitive dropped them. `role="tab"` joined the contract because G2's tab mode needs it and a primitive that lost it would turn a tab strip into a row of buttons for a screen reader with nothing visibly wrong. **Named gap:** the eight native `<select>`s are a separate decision — a shadcn Select is a button, a portal and a listbox rather than a `<select>`, and this package's dropdown carries §C5/§C6 lazy paging, search and an "other" row, so by the ADR's own admission rule it may not be a primitive at all. The file input is native for the same reason: `type="file"` has no value to hand a design system's Input |
| P3 | The playground: design, JSON and a live survey on one page, shareable by link | ◐ | `parity/P3-share-link` (unit). **Assembled from pieces, not from `<SurveyCreator>`** — which is the point of building it. [ADR-0021](./adr/0021-creator-composition.md) claims the Creator is "pieces with a default assembly on top" and that the assembly gets no privileged access; a layout the default assembly does not offer — a designer *beside* a live survey rather than one tab away from a preview — is the only way to find out. It is true: every part of the left pane is a public export taking a public model, and the site needed nothing the assembly has. **The two panes are one document.** Designer, JSON editor and live survey all come off a single `CreatorWorkspace`, so a question dropped on the canvas is answerable on the right with nothing wired between them — M3's preview session already watches the surface, and parses its own survey so answers can never reach what is being designed. Verified by hand: dropping a rating put an answerable rating in the live pane. **A link is the whole artefact** — base64url of the definition, nothing stored server-side, so it works offline and there is no state to expire. Base64url rather than percent-encoding because a definition is full of braces and every chat client guesses a different end for a URL containing `%7B`; the UTF-8 step is there because `btoa` takes latin1 and the survey somebody most wants to share is the one titled in their own script. A damaged link opens the starter survey rather than an error page. **Partial, and the status says so:** the codec is proven, the layout is not. `apps/site` has no end-to-end project — Playwright is wired to the built host-demo alone, and pointing it at a second app means a second web server plus establishing that TanStack Start's built output serves the way `vite preview` serves the demo's. That is real work rather than a line of config, and claiming the row green on a manual check would be exactly the assertion this document forbids. The unit project now includes `apps/*` so the site can carry proofs at all |
| P4 | A `Menu` primitive, and K5's element actions drawn with it | ☐ | Proposed by the reference application. K5's adorner draws Duplicate, Copy, Paste, Delete and a type picker as a row of buttons, which is as wide as its actions while the canvas is as wide as the host's layout gave it — so on any layout with a sidebar the row runs past the element. The immediate overflow is fixed in CSS (`flex-wrap` on the adorner, `min-width: 0` on the title); **collapsing the actions behind a `⋯` menu is the better design at any width**, and needs a primitive the set does not have. **The amendment's rule decides it, on first contact with a new case.** Radix's DropdownMenu is Root/Trigger/Content/Item — composed, like Select. Apply the interleaving test: the items are contiguous and the library draws nothing between them, so the host's component can own the whole composition and the contract is `items: {label, onSelect, isDestructive}[]` as *data*. That is a **leaf**, and admissible — unlike `RadioGroup`, which failed because the library interleaves table cells between items. That the rule settled a case it was not written for is the reason to trust it. **Sized honestly: this is a row, not a tweak.** [ADR-0022](./adr/0022-design-system-primitives.md) requires the shipped default to be behaviourally correct, and a menu means focus management, Escape, arrow keys, click-outside and returning focus to the trigger — built from native elements with no dependencies, which is the same class of work as a Dialog and why the original set stopped at five. Justified because K5's actions will not be the only caller: K4's page navigator wants the same control. The reference application supplies ReUI's dropdown as its adapter; the library depends on none of it |
| P5 | Every refusal carries a reason, and every automatic action is announced ([ADR-0023](./adr/0023-the-creator-says-what-happened.md)) | ◐ | `parity/P5-refusal-reasons`, `parity/P5-refusal-words` (unit) and `parity/P5-refusal-shown` (browser, in real Chromium). **The refusal half is done.** Every edit on `DesignSurface` returns `EditRefusal | undefined` instead of a boolean — ten reasons, each with words in the N3 catalogue, and `refusalMessageKey` is a total `Record` so a reason nobody gave words to fails to compile. `nameRefusal` is ADR-0023's one predicate: the rename guards with it and the field asks it, so the two cannot drift. **Read-only is minted once**, at `applyEdit` and `change`, so N2's restriction reaches edits that have never heard of it — and a new edit inherits it by construction. The grid shows the reason on the field with `aria-invalid` and `aria-describedby`, plus `role="alert"` because a blur-committed refusal lands after focus has left and nothing would re-read the description. Converting the 35 existing `toBe(true)`/`toBe(false)` assertions found two real defects the boolean had hidden: `setPropertyOn` discarded what the chokepoint returned, so read-only was silent, and `TranslationSession`'s `&& setText(…)` counted every success as a failure once success became `undefined`. **Open:** the automatic-action half — starter defaults on a drop, undo moving the selection, applying JSON replacing the document — and `PlacementSession`'s own `'refused'` vocabulary, which cannot yet distinguish "nothing to do" from "forbidden", so a drop onto its own position reports `not-placeable` |


## §O — Watch items (SurveyJS 2026 roadmap; not acceptance)

Image Map question; Fill-in-the-Blanks question; CSS-grid matrix overhaul; grid
layout engine; data-grid question with server-side processing; Tailwind/Bootstrap
style adapters; AI theme generation; rich-text authoring in Creator; refreshed
Dashboard/PDF (separate products). Revisit at each phase boundary; promote to a
lettered section only with evidence a real consumer needs it.

## Parent and related links

- [Project context](../CONTEXT.md)
- [North Star](./NORTH_STAR.md)
- [Delivery roadmap](./delivery-roadmap.md)
- [Library development guidelines](./library-development-guidelines-details.md)
- [Architecture decision records](./adr/README.md)
